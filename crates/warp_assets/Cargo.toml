[package]
name = "warp_assets"
version = "0.0.0"
edition = "2024"
authors.workspace = true
license.workspace = true
publish.workspace = true

[features]
standalone = []

[dependencies]
anyhow.workspace = true
rust-embed.workspace = true
warpui_core.workspace = true

[target.'cfg(target_family = "wasm")'.dependencies]
# Make sure we embed the data into the wasm binary even when running in debug
# mode, as it cannot be read directly from the filesystem.
rust-embed = { workspace = true, features = ["debug-embed"] }

[target.'cfg(target_env = "ohos")'.dependencies]
# The debug binary runs on-device, where app/assets is not on the filesystem,
# so the data must be embedded for debug builds just as it is for release.
rust-embed = { workspace = true, features = ["debug-embed"] }
