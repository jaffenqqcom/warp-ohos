diff --git a/.cargo/config.toml b/.cargo/config.toml
index a145971..2b254c7 100644
--- a/.cargo/config.toml
+++ b/.cargo/config.toml
@@ -16,3 +16,9 @@ git-fetch-with-cli = true
 
 [target.'cfg(target_family = "wasm")']
 rustflags = ["--cfg=web_sys_unstable_apis"]
+
+# === OHOS patched crates ===
+[patch.crates-io]
+nix = { path = "../depends/nix" }
+interprocess = { path = "../depends/interprocess" }
+gettext-sys = { path = "../depends/gettext-sys" }
diff --git a/Cargo.lock b/Cargo.lock
old mode 100644
new mode 100755
index 76553fb..5897a55
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -4,9 +4,9 @@ version = 4
 
 [[package]]
 name = "ab_glyph"
-version = "0.2.29"
+version = "0.2.32"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "ec3672c180e71eeaaac3a541fbbc5f5ad4def8b747c595ad30d674e43049f7b0"
+checksum = "01c0457472c38ea5bd1c3b5ada5e368271cb550be7a4ca4a0b4634e9913f6cc2"
 dependencies = [
  "ab_glyph_rasterizer",
  "owned_ttf_parser",
@@ -14,9 +14,9 @@ dependencies = [
 
 [[package]]
 name = "ab_glyph_rasterizer"
-version = "0.1.8"
+version = "0.1.10"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "c71b1793ee61086797f5c80b6efa2b8ffa6d5dd703f118545808a7f2e27f7046"
+checksum = "366ffbaa4442f4684d91e2cd7c5ea7c4ed8add41959a31447066e279e432b618"
 
 [[package]]
 name = "actix-codec"
@@ -1101,9 +1101,9 @@ checksum = "7c02d123df017efcdfbd739ef81735b36c5ba83ec3c59c80a9d7ecc718f92e50"
 
 [[package]]
 name = "as-raw-xcb-connection"
-version = "1.0.0"
+version = "1.0.1"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "2d5f312b0a56c5cdf967c0aeb67f6289603354951683bc97ddc595ab974ba9aa"
+checksum = "175571dd1d178ced59193a6fc02dde1b972eb0bc56c892cde9beeceac5bf0f6b"
 
 [[package]]
 name = "as-slice"
@@ -1126,7 +1126,7 @@ version = "0.38.0+1.3.281"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "0bb44936d800fea8f016d7f2311c6a4f97aebd5dc86f09906139ec848cf3a46f"
 dependencies = [
- "libloading",
+ "libloading 0.8.9",
 ]
 
 [[package]]
@@ -2761,7 +2761,7 @@ checksum = "0b023947811758c97c59bf9d1c188fd619ad4718dcaa767947df1cadb14f39f4"
 dependencies = [
  "glob",
  "libc",
- "libloading",
+ "libloading 0.8.9",
 ]
 
 [[package]]
@@ -3241,6 +3241,15 @@ version = "0.4.0"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "6245d59a3e82a7fc217c5828a6692dbc6dfb63a0c8c90495621f7b9d79704a0e"
 
+[[package]]
+name = "convert_case"
+version = "0.6.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "ec182b0ca2f35d8fc196cf3404988fd8b8c739a4d270ff118a398feb0cbec1ca"
+dependencies = [
+ "unicode-segmentation",
+]
+
 [[package]]
 name = "convert_case"
 version = "0.7.1"
@@ -3250,6 +3259,15 @@ dependencies = [
  "unicode-segmentation",
 ]
 
+[[package]]
+name = "convert_case"
+version = "0.11.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "affbf0190ed2caf063e3def54ff444b449371d55c58e513a95ab98eca50adb49"
+dependencies = [
+ "unicode-segmentation",
+]
+
 [[package]]
 name = "core-foundation"
 version = "0.9.4"
@@ -3711,6 +3729,12 @@ dependencies = [
  "syn 1.0.109",
 ]
 
+[[package]]
+name = "ctor"
+version = "1.0.9"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "a394189d59f9befacce833f337f7b1eca5e9a91221bcdd4d28e0114d96e597b3"
+
 [[package]]
 name = "ctr"
 version = "0.9.2"
@@ -4363,7 +4387,7 @@ version = "0.5.2"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "330c60081dcc4c72131f8eb70510f1ac07223e5d4163db481a04a0befcffa412"
 dependencies = [
- "libloading",
+ "libloading 0.8.9",
 ]
 
 [[package]]
@@ -4407,7 +4431,6 @@ checksum = "117240f60069e65410b3ae1bb213295bd828f707b5bec6596a1afc8793ce0cbc"
 [[package]]
 name = "dpi"
 version = "0.1.1"
-source = "git+https://github.com/warpdotdev/winit.git?rev=a4e0ecb5f9626ccac9445a73dc28354b52423abc#a4e0ecb5f9626ccac9445a73dc28354b52423abc"
 
 [[package]]
 name = "dsl_auto_type"
@@ -5573,8 +5596,6 @@ dependencies = [
 [[package]]
 name = "gettext-sys"
 version = "0.21.3"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "c63ce2e00f56a206778276704bbe38564c8695249fdc8f354b4ef71c57c3839d"
 dependencies = [
  "cc",
  "temp-dir",
@@ -6856,8 +6877,6 @@ checksum = "71dd52191aae121e8611f1e8dc3e324dd0dd1dee1e6dd91d10ee07a3cfb4d9d8"
 [[package]]
 name = "interprocess"
 version = "1.2.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "81f2533f3be42fffe3b5e63b71aeca416c1c3bc33e4e27be018521e76b1f38fb"
 dependencies = [
  "blocking",
  "cfg-if",
@@ -7267,7 +7286,7 @@ source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "6aae1df220ece3c0ada96b8153459b67eebe9ae9212258bb0134ae60416fdf76"
 dependencies = [
  "libc",
- "libloading",
+ "libloading 0.8.9",
  "pkg-config",
 ]
 
@@ -7425,6 +7444,16 @@ dependencies = [
  "windows-link 0.2.1",
 ]
 
+[[package]]
+name = "libloading"
+version = "0.9.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "754ca22de805bb5744484a5b151a9e1a8e837d5dc232c2d7d8c2e3492edc8b60"
+dependencies = [
+ "cfg-if",
+ "windows-link 0.2.1",
+]
+
 [[package]]
 name = "libm"
 version = "0.2.15"
@@ -8240,9 +8269,7 @@ checksum = "2195bf6aa996a481483b29d62a7663eed3fe39600c460e323f8ff41e90bdd89b"
 
 [[package]]
 name = "naga"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "aa2630921705b9b01dcdd0b6864b9562ca3c1951eecd0f0c4f5f04f61e412647"
+version = "29.0.3"
 dependencies = [
  "arrayvec",
  "bit-set 0.9.1",
@@ -8264,6 +8291,63 @@ dependencies = [
  "unicode-ident",
 ]
 
+[[package]]
+name = "napi-build-ohos"
+version = "1.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "617bcd176a3dc3d96c0acad112a482f592242d6fba6773a9eb8d90e042a35260"
+
+[[package]]
+name = "napi-derive-backend-ohos"
+version = "1.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "5b06bf282ab442eed04c89f8a833bd06d4cf22f18654f50d848449db9e94438a"
+dependencies = [
+ "convert_case 0.11.0",
+ "proc-macro2",
+ "quote",
+ "semver",
+ "syn 2.0.117",
+]
+
+[[package]]
+name = "napi-derive-ohos"
+version = "1.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "419b1721d9f21d76c1a5cb38e2628211cb786b5efb9e1dd2fc049ba1cb938e47"
+dependencies = [
+ "convert_case 0.11.0",
+ "ctor 1.0.9",
+ "napi-derive-backend-ohos",
+ "proc-macro2",
+ "quote",
+ "syn 2.0.117",
+]
+
+[[package]]
+name = "napi-ohos"
+version = "1.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "fe1a014105134b7889e85f5ab6f8ec5240db43ed37e6c4a38bd7b4a164b345e0"
+dependencies = [
+ "bitflags 2.9.4",
+ "ctor 1.0.9",
+ "futures",
+ "napi-build-ohos",
+ "napi-sys-ohos",
+ "nohash-hasher",
+ "rustc-hash 2.1.1",
+]
+
+[[package]]
+name = "napi-sys-ohos"
+version = "1.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "266964b97e57f3ca023ed13e7c78d4c8f3900aa00466bbeb8cb7746653cabc5f"
+dependencies = [
+ "libloading 0.9.0",
+]
+
 [[package]]
 name = "nasm-rs"
 version = "0.3.2"
@@ -8376,8 +8460,6 @@ checksum = "e4a24736216ec316047a1fc4252e27dabb04218aa4a3f37c6e7ddbf1f9782b54"
 [[package]]
 name = "nix"
 version = "0.26.4"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "598beaf3cc6fdd9a5dfb1630c2800c7acd31df7aaf0f565796fba2b53ca1af1b"
 dependencies = [
  "bitflags 1.3.2",
  "cfg-if",
@@ -8444,6 +8526,12 @@ dependencies = [
  "zip 2.4.2",
 ]
 
+[[package]]
+name = "nohash-hasher"
+version = "0.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "2bf50223579dc7cdcfb3bfcacf7069ff68243f8c363f62ffa99cf000a6b9c451"
+
 [[package]]
 name = "nom"
 version = "7.1.3"
@@ -9111,6 +9199,141 @@ dependencies = [
  "memchr",
 ]
 
+[[package]]
+name = "ohos-arkui-binding"
+version = "0.1.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "9352f0db69271fc23fb565ad1f034c2118a9ea9c727ece344834b451fe2c2865"
+dependencies = [
+ "bitflags 2.9.4",
+ "napi-ohos",
+ "napi-sys-ohos",
+ "ohos-arkui-sys",
+ "ohos-xcomponent-binding",
+ "ohos-xcomponent-sys",
+ "ohos_enum_macro 0.0.2",
+]
+
+[[package]]
+name = "ohos-arkui-sys"
+version = "0.0.1"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "1a19176486bbe46d33830aca4213b07bb46c745200118e57106639db4962e1c3"
+dependencies = [
+ "napi-sys-ohos",
+]
+
+[[package]]
+name = "ohos-display-binding"
+version = "0.0.1"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "157fe09232fd0af55ed9207d30b47a5f5ff7f84a0673a3b47f65acb7bbc9b8a9"
+dependencies = [
+ "ohos-display-sys",
+ "ohos_enum_macro 0.0.1",
+]
+
+[[package]]
+name = "ohos-display-sys"
+version = "0.0.1"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "fe9e1bba4d94159d0a65f1b4e748c148d4bce7930912c424522693f1837dad77"
+
+[[package]]
+name = "ohos-ime-binding"
+version = "0.1.2"
+dependencies = [
+ "ohos-input-method-sys",
+ "ohos_enum_macro 0.1.0",
+]
+
+[[package]]
+name = "ohos-input-method-sys"
+version = "0.1.1"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "225be105b169f45a2bca86498988c1877568d82542c013b345d605eb915e48ae"
+
+[[package]]
+name = "ohos-resource-manager-binding"
+version = "0.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "33793d5fc06cf356b3d25aa6228785e62c9f2a0e913a2c285b1eb8efb7185b5e"
+dependencies = [
+ "napi-ohos",
+ "ohos-resource-manager-sys",
+]
+
+[[package]]
+name = "ohos-resource-manager-sys"
+version = "0.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "97398d3f86b80279f062bd872e04be851fc9e0eb1dba4dcaabef872201517ff6"
+dependencies = [
+ "napi-sys-ohos",
+]
+
+[[package]]
+name = "ohos-xcomponent-binding"
+version = "0.2.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "6c5a9bb0fd157dc702863fe6b5e15b91a4ffdc08b4c47d57d080adcc73c36fa8"
+dependencies = [
+ "napi-derive-ohos",
+ "napi-ohos",
+ "napi-sys-ohos",
+ "ohos-xcomponent-sys",
+ "ohos_enum_macro 0.0.2",
+ "raw-window-handle 0.6.2",
+]
+
+[[package]]
+name = "ohos-xcomponent-sys"
+version = "0.0.2"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "5613209329ff61bc8695871ef985d57fc59ea4d55263e2c2395d8810d063575e"
+dependencies = [
+ "ohos-arkui-sys",
+]
+
+[[package]]
+name = "ohos_enum_macro"
+version = "0.0.1"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "b0ee769776099c824423d1686e21b5fd13b464d374d60f7cc25c48c1c836ede1"
+dependencies = [
+ "convert_case 0.6.0",
+ "proc-macro2",
+ "quote",
+ "regex",
+ "syn 2.0.117",
+]
+
+[[package]]
+name = "ohos_enum_macro"
+version = "0.0.2"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "b4bacd60b52be93024c446f27645d795ab71eb2243bb4c5d87b6238a35341fbd"
+dependencies = [
+ "convert_case 0.6.0",
+ "proc-macro2",
+ "quote",
+ "regex",
+ "syn 2.0.117",
+]
+
+[[package]]
+name = "ohos_enum_macro"
+version = "0.1.0"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "2fc1544b50988de3d636f971135e66c40479ef68e30ef09b15fb911763aab077"
+dependencies = [
+ "convert_case 0.6.0",
+ "proc-macro2",
+ "quote",
+ "regex",
+ "syn 2.0.117",
+]
+
 [[package]]
 name = "onboarding"
 version = "0.1.0"
@@ -9219,6 +9442,29 @@ dependencies = [
  "walkdir",
 ]
 
+[[package]]
+name = "openharmony-ability"
+version = "0.3.0"
+dependencies = [
+ "futures-channel",
+ "napi-derive-ohos",
+ "napi-ohos",
+ "ohos-arkui-binding",
+ "ohos-display-binding",
+ "ohos-ime-binding",
+ "ohos-resource-manager-binding",
+ "ohos-xcomponent-binding",
+]
+
+[[package]]
+name = "openharmony-ability-derive"
+version = "0.3.0"
+dependencies = [
+ "proc-macro2",
+ "quote",
+ "syn 2.0.117",
+]
+
 [[package]]
 name = "openssl"
 version = "0.10.79"
@@ -14449,7 +14695,7 @@ dependencies = [
  "crash-handler",
  "crc",
  "csv",
- "ctor",
+ "ctor 0.1.26",
  "cvt",
  "cynic",
  "dashmap",
@@ -14529,6 +14775,8 @@ dependencies = [
  "mio",
  "mockall",
  "mockito",
+ "napi-derive-ohos",
+ "napi-ohos",
  "natural_language_detection",
  "nix 0.26.4",
  "notify-debouncer-full",
@@ -14542,6 +14790,8 @@ dependencies = [
  "objc2-foundation 0.3.2",
  "onboarding",
  "once_cell",
+ "openharmony-ability",
+ "openharmony-ability-derive",
  "ordered-float 3.9.2",
  "os_info",
  "palette",
@@ -15298,6 +15548,8 @@ dependencies = [
  "objc2-metal 0.3.2",
  "objc2-quartz-core 0.3.2",
  "open",
+ "openharmony-ability",
+ "openharmony-ability-derive",
  "ordered-float 3.9.2",
  "owned_ttf_parser",
  "parking_lot",
@@ -15351,7 +15603,7 @@ dependencies = [
  "chrono",
  "command",
  "concat-idents",
- "ctor",
+ "ctor 0.1.26",
  "ctrlc",
  "dashmap",
  "derivative",
@@ -15621,11 +15873,11 @@ dependencies = [
 
 [[package]]
 name = "wayland-cursor"
-version = "0.31.8"
+version = "0.31.11"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "a93029cbb6650748881a00e4922b076092a6a08c11e7fbdb923f064b23968c5d"
+checksum = "447ccc440a881271b19e9989f75726d60faa09b95b0200a9b7eb5cc47c3eeb29"
 dependencies = [
- "rustix 0.38.44",
+ "rustix 1.1.2",
  "wayland-client",
  "xcursor",
 ]
@@ -15808,9 +16060,7 @@ dependencies = [
 
 [[package]]
 name = "wgpu"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "72c239a9a747bbd379590985bac952c2e53cb19873f7072b3370c6a6a8e06837"
+version = "29.0.3"
 dependencies = [
  "arrayvec",
  "bitflags 2.9.4",
@@ -15837,9 +16087,7 @@ dependencies = [
 
 [[package]]
 name = "wgpu-core"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "1e80ac6cf1895df6342f87d975162108f9d98772a0d74bc404ab7304ac29469e"
+version = "29.0.3"
 dependencies = [
  "arrayvec",
  "bit-set 0.9.1",
@@ -15871,45 +16119,35 @@ dependencies = [
 
 [[package]]
 name = "wgpu-core-deps-apple"
-version = "29.0.0"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "43acd053312501689cd92a01a9638d37f3e41a5fd9534875efa8917ee2d11ac0"
+version = "29.0.3"
 dependencies = [
  "wgpu-hal",
 ]
 
 [[package]]
 name = "wgpu-core-deps-emscripten"
-version = "29.0.0"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "ef043bf135cc68b6f667c55ff4e345ce2b5924d75bad36a47921b0287ca4b24a"
+version = "29.0.3"
 dependencies = [
  "wgpu-hal",
 ]
 
 [[package]]
 name = "wgpu-core-deps-wasm"
-version = "29.0.0"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "2f7b75e72f49035f000dd5262e4126242e92a090a4fd75931ecfe7e60784e6fa"
+version = "29.0.3"
 dependencies = [
  "wgpu-hal",
 ]
 
 [[package]]
 name = "wgpu-core-deps-windows-linux-android"
-version = "29.0.0"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "725d5c006a8c02967b6d93ef04f6537ec4593313e330cfe86d9d3f946eb90f28"
+version = "29.0.3"
 dependencies = [
  "wgpu-hal",
 ]
 
 [[package]]
 name = "wgpu-hal"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "89a47aef47636562f3937285af4c44b4b5b404b46577471411cc5313a921da7e"
+version = "29.0.3"
 dependencies = [
  "android_system_properties",
  "arrayvec",
@@ -15928,7 +16166,7 @@ dependencies = [
  "js-sys",
  "khronos-egl",
  "libc",
- "libloading",
+ "libloading 0.8.9",
  "log",
  "naga",
  "ndk-sys",
@@ -15956,13 +16194,12 @@ dependencies = [
  "wgpu-types",
  "windows 0.62.2",
  "windows-core 0.62.2",
+ "windows-result 0.4.1",
 ]
 
 [[package]]
 name = "wgpu-naga-bridge"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "7b4684f4410da0cf95a4cb63bb5edaac022461dedb6adf0b64d0d9b5f6890d51"
+version = "29.0.3"
 dependencies = [
  "naga",
  "wgpu-types",
@@ -15970,9 +16207,7 @@ dependencies = [
 
 [[package]]
 name = "wgpu-types"
-version = "29.0.1"
-source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "ec2675540fb1a5cfa5ef122d3d5f390e2c75711a0b946410f2d6ac3a0f77d1f6"
+version = "29.0.3"
 dependencies = [
  "bitflags 2.9.4",
  "bytemuck",
@@ -16465,7 +16700,6 @@ checksum = "589f6da84c646204747d1270a2a5661ea66ed1cced2631d546fdfb155959f9ec"
 [[package]]
 name = "winit"
 version = "0.30.13"
-source = "git+https://github.com/warpdotdev/winit.git?rev=a4e0ecb5f9626ccac9445a73dc28354b52423abc#a4e0ecb5f9626ccac9445a73dc28354b52423abc"
 dependencies = [
  "ahash 0.8.11",
  "android-activity",
@@ -16488,6 +16722,8 @@ dependencies = [
  "objc2-app-kit 0.2.2",
  "objc2-foundation 0.2.2",
  "objc2-ui-kit",
+ "openharmony-ability",
+ "openharmony-ability-derive",
  "orbclient",
  "percent-encoding",
  "pin-project",
@@ -16736,7 +16972,7 @@ dependencies = [
  "as-raw-xcb-connection",
  "gethostname",
  "libc",
- "libloading",
+ "libloading 0.8.9",
  "once_cell",
  "rustix 1.1.2",
  "x11rb-protocol",
@@ -16770,12 +17006,9 @@ dependencies = [
 
 [[package]]
 name = "xcursor"
-version = "0.3.4"
+version = "0.3.10"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "463705a63313cd4301184381c5e8042f0a7e9b4bb63653f216311d4ae74690b7"
-dependencies = [
- "nom 7.1.3",
-]
+checksum = "bec9e4a500ca8864c5b47b8b482a73d62e4237670e5b5f1d6b9e3cae50f28f2b"
 
 [[package]]
 name = "xdg"
@@ -17229,3 +17462,7 @@ dependencies = [
  "syn 2.0.117",
  "winnow 0.7.12",
 ]
+
+[[patch.unused]]
+name = "wgpu-macros"
+version = "29.0.3"
diff --git a/Cargo.toml b/Cargo.toml
index 17e3c88..45b225a 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -363,7 +363,7 @@ rayon = "1.10.0"
 sha2 = "0.10"
 shellexpand = "3.1.1"
 warp-command-signatures = { git = "https://github.com/warpdotdev/command-signatures.git", rev = "a1000677e302b8fdcefb08365f3f7a81bbba23b3", default-features = false }
-winit = { git = "https://github.com/warpdotdev/winit.git", rev = "a4e0ecb5f9626ccac9445a73dc28354b52423abc" }
+winit = { path = "../depends/winit" }
 x11rb = "0.13.0"
 mockito = "1.7.0"
 sysinfo = { version = "0.37.0", default-features = false, features = [
@@ -528,6 +528,17 @@ tink-hybrid = { git = "https://github.com/warpdotdev/tink-rust", branch = "warpd
 tikv-jemallocator = { git = "https://github.com/warpdotdev/jemallocator.git", rev = "2ee30bfdf7059223b54810e4ea6c666f0a379e0b" }
 tikv-jemalloc-sys = { git = "https://github.com/warpdotdev/jemallocator.git", rev = "2ee30bfdf7059223b54810e4ea6c666f0a379e0b" }
 
+# OHOS 适配 patch：修复 nix/interprocess/gettext-sys 在 OHOS 目标下的编译问题
+nix = { path = "patches/nix" }
+interprocess = { path = "patches/interprocess" }
+gettext-sys = { path = "patches/gettext-sys" }
+
+# OHOS 适配 patch：使用本地 wgpu fork（含 OHOS 后端支持）
+# 与 winit 等一致，放在 depends/wgpu/ 下
+wgpu = { path = "../depends/wgpu/wgpu" }
+wgpu-core = { path = "../depends/wgpu/wgpu-core" }
+wgpu-hal = { path = "../depends/wgpu/wgpu-hal" }
+wgpu-types = { path = "../depends/wgpu/wgpu-types" }
+wgpu-macros = { path = "../depends/wgpu/wgpu-macros" }
+
 [patch."https://github.com/warpdotdev/warp-proto-apis.git"]
-# Uncomment for local development of warp-proto-apis
-# warp_multi_agent_api = { path = "../warp-proto-apis/apis/multi_agent/v1/gen/rust" }
diff --git a/app/Cargo.toml b/app/Cargo.toml
index 61ac931..2649afa 100644
--- a/app/Cargo.toml
+++ b/app/Cargo.toml
@@ -12,6 +12,13 @@ license.workspace = true
 [lib]
 name = "warp"
 path = "src/lib.rs"
+crate-type = ["lib", "cdylib"]
+
+[target.'cfg(target_env = "ohos")'.dependencies]
+napi-ohos = { version = "1.1", features = ["napi8"] }
+napi-derive-ohos = { version = "1.1" }
+openharmony-ability = { path = "../../depends/openharmony-ability/crates/ability" }
+openharmony-ability-derive = { path = "../../depends/openharmony-ability/crates/derive" }
 
 # We define separate targets for each channel (stable, preview, dev) so that we can
 # bundle with its own icon, app name, and channel-specific configuration (such as
diff --git a/app/src/appearance.rs b/app/src/appearance.rs
index a4f43bc..9b56c02 100644
--- a/app/src/appearance.rs
+++ b/app/src/appearance.rs
@@ -273,7 +273,12 @@ impl Entity for AppearanceManager {
 impl SingletonEntity for AppearanceManager {}
 
 fn load_default_monospace_font_family(ctx: &mut AppContext) -> anyhow::Result<FamilyId> {
-    warpui::fonts::Cache::handle(ctx).update(ctx, |font_cache, _| {
+    log::info!("[BOOT] load_default_monospace_font: getting Cache handle");
+    let cache_handle = warpui::fonts::Cache::handle(ctx);
+    log::info!("[BOOT] load_default_monospace_font: calling Cache::update");
+    cache_handle.update(ctx, |font_cache, _| {
+        log::info!("[BOOT] load_default_monospace_font: update closure entered");
+        log::info!("[BOOT] load_default_monospace_font: loading Hack font family from bytes");
         let default_monospace_font_family = font_cache.load_family_from_bytes(
             "Hack",
             vec![
@@ -285,8 +290,11 @@ fn load_default_monospace_font_family(ctx: &mut AppContext) -> anyhow::Result<Fa
                     .to_vec(),
             ],
         )?;
+        log::info!("[BOOT] load_default_monospace_font: Hack family loaded, selecting font");
         let default_monospace_font =
             font_cache.select_font(default_monospace_font_family, Default::default());
+        log::info!("[BOOT] load_default_monospace_font: font selected");
+        log::info!("[BOOT] load_default_monospace_font: getting glyph typographic bounds");
         font_cache.glyph_typographic_bounds(
             default_monospace_font,
             MonospaceFontSize::default_value(),
@@ -295,6 +303,7 @@ fn load_default_monospace_font_family(ctx: &mut AppContext) -> anyhow::Result<Fa
                 .ok_or_else(|| anyhow!("monospace font has no 'm' glyph"))?
                 .0,
         )?;
+        log::info!("[BOOT] load_default_monospace_font: all done, returning");
         Ok(default_monospace_font_family)
     })
 }
@@ -387,35 +396,52 @@ fn get_or_load_font_family(font_name: &str, ctx: &mut AppContext) -> Option<Fami
 }
 
 fn build_appearance(ctx: &mut AppContext) -> Appearance {
+    log::info!("[BOOT] build_appearance: entry");
+    log::info!("[BOOT] build_appearance: loading default monospace font family (Hack)");
     let default_monospace_font_family = load_default_monospace_font_family(ctx)
         .expect("unable to load default monospace font family");
+    log::info!("[BOOT] build_appearance: Hack loaded OK");
+    log::info!("[BOOT] build_appearance: reading FontSettings");
     let monospace_font_name = FontSettings::as_ref(ctx)
         .monospace_font_name
         .value()
         .clone();
+    log::info!("[BOOT] build_appearance: monospace_font_name done");
     let am_font_name = FontSettings::as_ref(ctx).ai_font_name.value().clone();
+    log::info!("[BOOT] build_appearance: ai_font_name done");
 
+    log::info!("[BOOT] build_appearance: get_or_load_font_family(monospace)");
     let monospace_font_family_from_settings = get_or_load_font_family(&monospace_font_name, ctx);
+    log::info!("[BOOT] build_appearance: monospace font family done");
 
+    log::info!("[BOOT] build_appearance: loading Roboto UI font");
     let ui_font_family =
         load_default_ui_font_family(ctx).expect("unable to load default ui font family");
+    log::info!("[BOOT] build_appearance: Roboto loaded OK");
 
+    log::info!("[BOOT] build_appearance: get_or_load_font_family(ai_font)");
     let am_font_family_from_settings = get_or_load_font_family(&am_font_name, ctx);
+    log::info!("[BOOT] build_appearance: ai font family done");
 
+    log::info!("[BOOT] build_appearance: loading password font");
     let password_font_family =
         load_password_font_family(ctx).expect("unable to load password font family");
+    log::info!("[BOOT] build_appearance: password font done");
 
+    log::info!("[BOOT] build_appearance: reading font size/weight/line_height");
     let monospace_font_size = *FontSettings::as_ref(ctx).monospace_font_size.value();
-
     let monospace_font_weight = *FontSettings::as_ref(ctx).monospace_font_weight.value();
-
     let line_height_ratio = *FontSettings::as_ref(ctx).line_height_ratio.value();
+    log::info!("[BOOT] build_appearance: font settings read done");
 
+    log::info!("[BOOT] build_appearance: reading theme");
     let theme_kind = active_theme_kind(ThemeSettings::as_ref(ctx), ctx);
     let theme = Settings::theme_for_theme_kind(&theme_kind, ctx);
+    log::info!("[BOOT] build_appearance: theme loaded");
     #[cfg(target_family = "wasm")]
     emit_theme_background_event(&theme);
 
+    log::info!("[BOOT] build_appearance: calling Appearance::new");
     Appearance::new(
         theme,
         monospace_font_family_from_settings.unwrap_or(default_monospace_font_family),
@@ -438,6 +464,22 @@ fn emit_theme_background_event(theme: &WarpTheme) {
 }
 
 pub fn register(app: &mut impl AddSingletonModel) {
-    app.add_singleton_model(|ctx| build_appearance(ctx));
-    app.add_singleton_model(AppearanceManager::new);
+    log::info!("[BOOT] appearance::register: entry");
+    log::info!("[BOOT] appearance::register: adding build_appearance singleton");
+    app.add_singleton_model(|ctx| {
+        log::info!("[BOOT] appearance::register: build_appearance closure called");
+        let result = build_appearance(ctx);
+        log::info!("[BOOT] appearance::register: build_appearance done");
+        result
+    });
+    log::info!("[BOOT] appearance::register: build_appearance singleton added");
+    log::info!("[BOOT] appearance::register: adding AppearanceManager singleton");
+    app.add_singleton_model(|ctx| {
+        log::info!("[BOOT] appearance::register: AppearanceManager::new closure called");
+        let result = AppearanceManager::new(ctx);
+        log::info!("[BOOT] appearance::register: AppearanceManager::new done");
+        result
+    });
+    log::info!("[BOOT] appearance::register: AppearanceManager singleton added");
+    log::info!("[BOOT] appearance::register: exit");
 }
diff --git a/app/src/features.rs b/app/src/features.rs
index 4cd1956..6f20d0d 100644
--- a/app/src/features.rs
+++ b/app/src/features.rs
@@ -22,6 +22,12 @@ fn enabled_features() -> HashSet<FeatureFlag> {
         flags.extend(RELEASE_FLAGS);
     }
 
+    // Enable OHOS-specific flags.
+    #[cfg(target_env = "ohos")]
+    {
+        flags.extend(OHOS_FLAGS);
+    }
+
     flags.extend([
         #[cfg(feature = "autoupdate")]
         FeatureFlag::Autoupdate,
diff --git a/app/src/lib.rs b/app/src/lib.rs
index eebccad..56d6a38 100644
--- a/app/src/lib.rs
+++ b/app/src/lib.rs
@@ -757,32 +757,38 @@ pub fn run_integration_test(driver: TestDriver) -> Result<()> {
 /// Runs the app (or CLI / daemon).
 fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
     let mut timer = IntervalTimer::new();
+    log::info!("[BOOT] run_internal: entry");
 
     // ── Early initialization (pre-AppBuilder) ──────────────────────
     // These steps run before the platform event loop is started.
     // They must not depend on AppContext.
 
     #[cfg(windows)]
-    dynamic_libraries::configure_library_loading();
+    {
+        log::info!("[BOOT] run_internal: configuring library loading (windows)");
+        dynamic_libraries::configure_library_loading();
+    }
 
     if launch_mode.needs_profiling() {
+        log::info!("[BOOT] run_internal: initializing profiling");
         profiling::init();
+    } else {
+        log::info!("[BOOT] run_internal: profiling not needed, skipping");
     }
 
     // The `run` function already initializes feature flags, but ensure they're initialized here
     // for other entrypoints.
+    log::info!("[BOOT] run_internal: initializing feature flags");
     features::init_feature_flags();
 
     #[cfg(feature = "crash_reporting")]
     if launch_mode.needs_crash_reporting() {
-        // Ensure that the main/root Sentry hub is initialized on the main
-        // thread.  PtySpawner creates a background thread to receive logs from
-        // the terminal server process, and we don't want it to be the host of
-        // the primary sentry::Hub.
+        log::info!("[BOOT] run_internal: initializing Sentry hub (crash reporting)");
         sentry::Hub::main();
     }
 
     if launch_mode.needs_profiling() {
+        log::info!("[BOOT] run_internal: initializing tracing");
         tracing::init()?;
     }
 
@@ -790,7 +796,9 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
     let is_cli = log_destination.is_some();
 
     cfg_if::cfg_if! {
-        if #[cfg(enable_crash_recovery)] {
+        if #[cfg(target_env = "ohos")] {
+            log::info!("[BOOT] run_internal: OHOS target, skipping file-based warp_logging::init(), using OhosLogger->HiLog");
+        } else if #[cfg(enable_crash_recovery)] {
             if crash_recovery::is_crash_recovery_process(launch_mode.args().as_ref()) {
                 warp_logging::init_for_crash_recovery_process()?;
             } else {
@@ -812,20 +820,19 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
     timer.mark_interval_end("LOG_FILE_SETUP_COMPLETE");
 
     #[cfg(windows)]
-    platform::windows::check_redirection_guard();
+    {
+        log::info!("[BOOT] run_internal: checking redirection guard (windows)");
+        platform::windows::check_redirection_guard();
+    }
 
-    // Adjust resource limits early, before doing other work, to ensure that
-    // any children we spawn (like the terminal server) inherit our adjusted
-    // rlimits.
+    log::info!("[BOOT] run_internal: adjusting resource limits");
     resource_limits::adjust_resource_limits();
 
-    // Configure rustls to use its default crypto provider.  This MUST be called
-    // before making any network requests that use TLS, otherwise rustls will
-    // panic.
+    log::info!("[BOOT] run_internal: initializing rustls crypto provider");
     #[cfg(not(target_family = "wasm"))]
     rustls::crypto::aws_lc_rs::default_provider()
         .install_default()
-        .expect("must be able to initialize crypto provider for TLS support");
+        .expect("[BOOT] run_internal: FAILED to initialize rustls crypto provider");
 
     // For wasm builds we have this special case to parse out the intent
     // from the url that is used to visite the app on web.
@@ -930,16 +937,26 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
     // files, modified signal handlers, etc.) to avoid unexpected effects on
     // spawned ptys.
     #[cfg(feature = "local_tty")]
-    let pty_spawner =
-        terminal::local_tty::spawner::PtySpawner::new().context("Failed to create pty spawner")?;
+    let pty_spawner = {
+        log::info!("[BOOT] run_internal: calling PtySpawner::new()");
+        let result = terminal::local_tty::spawner::PtySpawner::new();
+        match &result {
+            Ok(_) => log::info!("[BOOT] run_internal: PtySpawner::new() OK"),
+            Err(e) => log::error!("[BOOT] run_internal: PtySpawner::new() FAILED: {:?}", e),
+        }
+        result.context("Failed to create pty spawner")?
+    };
 
+    log::info!("[BOOT] run_internal: constructing AppBuilder");
     let mut app_builder = if launch_mode.is_headless() {
+        log::info!("[BOOT] run_internal: creating headless AppBuilder");
         warpui::platform::AppBuilder::new_headless(
             app_callbacks(launch_mode.is_integration_test()),
             Box::new(ASSETS),
             launch_mode.take_test_driver(),
         )
     } else {
+        log::info!("[BOOT] run_internal: creating normal (non-headless) AppBuilder");
         warpui::platform::AppBuilder::new(
             app_callbacks(launch_mode.is_integration_test()),
             Box::new(ASSETS),
@@ -963,8 +980,9 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
         app_builder.set_dock_menu_builder(|_| app_menus::dock_menu());
     }
 
-    #[cfg(any(target_os = "linux", target_os = "freebsd"))]
+    #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))]
     {
+        log::info!("[BOOT] run_internal: configuring Linux/FreeBSD AppBuilder (non-OHOS)");
         use warpui::platform::linux::{self, AppBuilderExt};
 
         use crate::settings::ForceX11;
@@ -973,53 +991,60 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
 
         let force_x11 = ForceX11::read_from_preferences(prefs_for_public_settings)
             .unwrap_or(ForceX11::default_value());
-        // Force use of wayland if the user has passed the `WARP_ENABLE_WAYLAND` env var.
         let allow_wayland = linux::is_wayland_env_var_set() || !force_x11;
+        log::info!("[BOOT] run_internal: force_x11={} allow_wayland={}", force_x11, allow_wayland);
         app_builder.force_x11(!allow_wayland);
     }
 
     #[cfg(target_os = "windows")]
     {
+        log::info!("[BOOT] run_internal: configuring Windows AppBuilder");
         use warpui::platform::windows::AppBuilderExt;
         app_builder.set_app_user_model_id(ChannelState::app_id().to_string());
 
-        // Only use DXC for DirectX shader compilation if we're not running in a Parallels VM
-        // Parallels VMs can have issues with DXC shader compilation
         let is_parallels_vm = crate::util::vm_detection::is_running_in_windows_parallels_vm();
         if !is_parallels_vm {
-            log::info!("Using DXC for DirectX shader compilation");
+            log::info!("[BOOT] run_internal: using DXC for DirectX shader compilation");
             use warpui::platform::windows::DXCPath;
-
             app_builder.use_dxc_for_directx_shader_compilation(DXCPath {
                 dxc_path: "dxcompiler.dll".to_string(),
                 dxil_path: "dxil.dll".to_string(),
             });
         } else {
-            log::info!("Skipping DXC for DirectX shader compilation; running in a Parallels VM");
+            log::info!("[BOOT] run_internal: skipping DXC, running in Parallels VM");
         }
     }
 
-    // Override any bindings that have a `Custom` trigger to a `Keystroke`-based trigger. In theory,
-    // this should be a noop on Mac (since the keystrokes registered via the  Mac menus first
-    // intercept the binding), but just to be safe we only enable this in cases where we don't
-    // include mac menus.
+    // Override any bindings
     #[cfg(not(target_os = "macos"))]
-    app_builder.convert_custom_triggers_to_keystroke_triggers(
-        crate::util::bindings::custom_tag_to_keystroke,
-    );
+    {
+        log::info!("[BOOT] run_internal: converting custom triggers to keystroke triggers");
+        app_builder.convert_custom_triggers_to_keystroke_triggers(
+            crate::util::bindings::custom_tag_to_keystroke,
+        );
+    }
 
     #[cfg(target_os = "macos")]
-    app_builder.register_default_keystroke_triggers_for_custom_actions(
-        crate::util::bindings::custom_tag_to_keystroke,
-    );
+    {
+        log::info!("[BOOT] run_internal: registering default keystroke triggers (macOS)");
+        app_builder.register_default_keystroke_triggers_for_custom_actions(
+            crate::util::bindings::custom_tag_to_keystroke,
+        );
+    }
+
+    log::info!("[BOOT] run_internal: calling app_builder.run() (entering event loop)");
+    app_builder.run(move |ctx: &mut warpui::AppContext| {
+        log::info!("[BOOT] run_internal: app_builder.run closure entered");
 
-    app_builder.run(move |ctx| {
         #[cfg(not(target_family = "wasm"))]
-        // Rotate the log files in the background.
-        ctx.background_executor()
-            .spawn(warp_logging::rotate_log_files())
-            .detach();
+        {
+            log::info!("[BOOT] run_internal: spawning log rotation");
+            ctx.background_executor()
+                .spawn(warp_logging::rotate_log_files())
+                .detach();
+        }
 
+        log::info!("[BOOT] run_internal: adding AppExecutionMode singleton");
         ctx.add_singleton_model(|ctx| {
             AppExecutionMode::new(
                 launch_mode.execution_mode(),
@@ -1028,14 +1053,18 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
             )
         });
         #[cfg(feature = "crash_reporting")]
-        crate::crash_reporting::set_client_type_tag(launch_mode.execution_mode().client_id());
+        {
+            log::info!("[BOOT] run_internal: setting client type tag (crash reporting)");
+            crate::crash_reporting::set_client_type_tag(launch_mode.execution_mode().client_id());
+        }
 
-        // Add the terminal server singleton to the application.
         #[cfg(feature = "local_tty")]
-        ctx.add_singleton_model(move |_ctx| pty_spawner);
+        {
+            log::info!("[BOOT] run_internal: adding PtySpawner singleton");
+            ctx.add_singleton_model(move |_ctx| pty_spawner);
+        }
 
-        // Register user preferences.  This must be done before initializing
-        // feature flags or experiments, both of which check user preferences for
+        log::info!("[BOOT] run_internal: adding PublicPreferences + PrivatePreferences singletons");
         // overrides.
         ctx.add_singleton_model(move |_ctx| ::settings::PublicPreferences::new(public_preferences));
         ctx.add_singleton_model(move |_ctx| private_preferences);
@@ -1048,6 +1077,7 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
         ctx.add_singleton_model(move |ctx| {
             plugin::PluginHost::new(ctx).expect("Could not instantiate PluginHost")
         });
+        log::info!("[BOOT] run_internal: calling initialize_app()");
         let app_state = initialize_app(
             &launch_mode,
             timer,
@@ -1055,12 +1085,16 @@ fn run_internal(mut launch_mode: LaunchMode) -> Result<()> {
             ctx,
             pre_sentry_errors,
         );
+        log::info!("[BOOT] run_internal: initialize_app() returned, app_state.is_some={}", app_state.is_some());
 
         if ImprovedPaletteSearch::improved_search_enabled(ctx) {
+            log::info!("[BOOT] run_internal: enabling Tantivy search");
             FeatureFlag::UseTantivySearch.set_enabled(true);
         }
 
+        log::info!("[BOOT] run_internal: calling launch()");
         launch(ctx, app_state, launch_mode);
+        log::info!("[BOOT] run_internal: launch() returned");
     })
 }
 
@@ -1075,20 +1109,25 @@ pub(crate) fn initialize_app(
     ctx: &mut warpui::AppContext,
     _pre_sentry_errors: impl IntoIterator<Item = anyhow::Error>,
 ) -> Option<AppState> {
-    // WARNING: Errors that happen here before crash_reporting::init will not be collected in
-    // Sentry. Only the dependencies of crash_reporting should be initialized here. Avoid adding
-    // any other stuff here, as failures will be silent. Push them to pre_sentry_errors instead.
+    log::info!("[BOOT] initialize_app: entry");
     let data_domain = ChannelState::data_domain();
 
     // Register an implementation of the secure storage service.
     cfg_if::cfg_if! {
         if #[cfg(feature = "integration_tests")] {
+            log::info!("[BOOT] initialize_app: registering noop secure storage (integration_tests)");
+            warpui_extras::secure_storage::register_noop(&data_domain, ctx);
+        } else if #[cfg(target_env = "ohos")] {
+            log::info!("[BOOT] initialize_app: OHOS platform, registering noop secure storage (no Secret Service)");
             warpui_extras::secure_storage::register_noop(&data_domain, ctx);
         } else if #[cfg(any(target_os = "linux", target_os = "freebsd"))] {
+            log::info!("[BOOT] initialize_app: registering secure storage with fallback (linux)");
             warpui_extras::secure_storage::register_with_fallback(&data_domain, warp_core::paths::state_dir(), ctx)
         } else if #[cfg(target_os = "windows")] {
+            log::info!("[BOOT] initialize_app: registering secure storage with dir (windows)");
             warpui_extras::secure_storage::register_with_dir(&data_domain, warp_core::paths::state_dir(), ctx)
         } else {
+            log::info!("[BOOT] initialize_app: registering default secure storage");
             warpui_extras::secure_storage::register(&data_domain, ctx);
         }
     }
@@ -1099,13 +1138,21 @@ pub(crate) fn initialize_app(
     #[cfg(target_os = "macos")]
     preview_config_migration::migrate_preview_config_dir_if_needed();
 
+    log::info!("[BOOT] initialize_app: ensure_warp_watch_roots_exist");
     ensure_warp_watch_roots_exist();
+
+    log::info!("[BOOT] initialize_app: adding WarpManagedPathsWatcher singleton");
     ctx.add_singleton_model(WarpManagedPathsWatcher::new);
+    log::info!("[BOOT] initialize_app: WarpManagedPathsWatcher added");
 
+    log::info!("[BOOT] initialize_app: adding WarpConfig singleton");
     ctx.add_singleton_model(WarpConfig::new);
+    log::info!("[BOOT] initialize_app: adding SettingsManager singleton");
     ctx.add_singleton_model(|_ctx| SettingsManager::default());
 
+    log::info!("[BOOT] initialize_app: calling settings::init");
     let user_defaults_on_startup = settings::init(startup_toml_parse_error, ctx);
+    log::info!("[BOOT] initialize_app: settings::init returned");
     timer.mark_interval_end("READ_USER_DEFAULTS_AND_INITIALIZE_SETTINGS");
 
     if FeatureFlag::UIZoom.is_enabled() {
@@ -1124,7 +1171,9 @@ pub(crate) fn initialize_app(
         None
     };
 
+    log::info!("[BOOT] initialize_app: AuthState::initialize start");
     let auth_state = Arc::new(AuthState::initialize(ctx, api_key));
+    log::info!("[BOOT] initialize_app: AuthState::initialize done");
     timer.mark_interval_end("AUTH_MANAGER_SET_USER");
 
     let agent_source = determine_agent_source(launch_mode);
@@ -1182,7 +1231,10 @@ pub(crate) fn initialize_app(
         | LaunchMode::RemoteServerProxy
         | LaunchMode::Test { .. } => persistence::PersistenceScope::App,
     };
+    log::info!("[BOOT] initialize_app: persistence::initialize");
     let (sqlite_data, writer_handles) = persistence::initialize(ctx, persistence_scope);
+    log::info!("[BOOT] initialize_app: persistence::initialize returned sqlite_data.is_some={}",
+        sqlite_data.is_some());
     timer.mark_interval_end("SQLITE_INITIALIZED");
 
     let persistence_writer = PersistenceWriter::new(writer_handles);
@@ -1445,15 +1497,16 @@ pub(crate) fn initialize_app(
     });
 
     let user_is_logged_in = auth_state.is_logged_in();
+    log::info!("[BOOT] initialize_app: user_is_logged_in={}", user_is_logged_in);
 
     if user_is_logged_in {
-        // Skip refresh_user for CLI mode — the CLI handles auth refresh in
-        // ensure_auth_state so it can detect invalid credentials before running
-        // a command.
         if !matches!(launch_mode, LaunchMode::CommandLine { .. }) {
+            log::info!("[BOOT] initialize_app: refreshing auth user");
             AuthManager::handle(ctx).update(ctx, |auth_manager, ctx| {
                 auth_manager.refresh_user(ctx);
             });
+        } else {
+            log::info!("[BOOT] initialize_app: skipping auth refresh (CLI mode)");
         }
 
         // Set the first frame callback to record the app's startup time.
@@ -2038,6 +2091,7 @@ pub(crate) fn initialize_app(
         http_server::HttpServer::new(routers, ctx)
     });
 
+    log::info!("[BOOT] initialize_app: exit app_state.is_some={}", app_state.is_some());
     app_state
 }
 
@@ -2484,10 +2538,12 @@ fn is_cloud_agent_web_home_launch_url(url: &Url) -> bool {
             .any(|(key, value)| key == "source" && value == "web_home")
 }
 fn launch(ctx: &mut warpui::AppContext, app_state: Option<AppState>, launch_mode: LaunchMode) {
+    log::info!("[BOOT] launch: entry app_state.is_some={}", app_state.is_some());
     IntervalTimer::handle(ctx).update(ctx, |timer, _ctx| {
         timer.mark_interval_end("APP_LAUNCHED");
     });
 
+    log::info!("[BOOT] launch: loading custom keybindings");
     keyboard::load_custom_keybindings(ctx);
 
     IntervalTimer::handle(ctx).update(ctx, |timer, _ctx| {
@@ -2498,28 +2554,38 @@ fn launch(ctx: &mut warpui::AppContext, app_state: Option<AppState>, launch_mode
     #[cfg(target_family = "wasm")]
     ctx.set_fallback_font_fn(font_fallback::fallback_font_fn);
 
-    match launch_mode {
+    log::info!("[BOOT] launch: matching launch_mode");
+    match &launch_mode {
         LaunchMode::App { .. } | LaunchMode::Test { .. } => {
             let should_skip_restore = launch_mode
                 .args()
                 .urls
                 .iter()
                 .any(is_cloud_agent_web_home_launch_url);
+            log::info!("[BOOT] launch: App/Test mode, should_skip_restore={}", should_skip_restore);
             let app_state = if should_skip_restore { None } else { app_state };
             // Attempt to restore windows from the persisted application state.
+            log::info!("[BOOT] launch: dispatching root_view:open_from_restored");
             let arg = OpenFromRestoredArg { app_state };
             ctx.dispatch_global_action("root_view:open_from_restored", &arg);
 
-            // Process any URLs that were provided on the command line (which may be
-            // file:// URLs or ones using our custom URL scheme).
-            for url in launch_mode.args().urls.iter() {
+            // Process any URLs that were provided on the command line
+            let url_count = launch_mode.args().urls.len();
+            log::info!("[BOOT] launch: processing {} URLs from command line", url_count);
+            for (i, url) in launch_mode.args().urls.iter().enumerate() {
+                log::info!("[BOOT] launch: URI[{}]={}", i, url);
                 uri::handle_incoming_uri(url, ctx);
             }
 
             // If, after session restoration and command-line argument handling, we
             // haven't opened any windows, open a new window.
-            if ctx.window_ids().count() == 0 {
+            let window_count = ctx.window_ids().count();
+            log::info!("[BOOT] launch: window count after restore/urls={}", window_count);
+            if window_count == 0 {
+                log::info!("[BOOT] launch: no windows open, dispatching root_view:open_new");
                 ctx.dispatch_global_action("root_view:open_new", &());
+            } else {
+                log::info!("[BOOT] launch: windows already exist, skipping open_new");
             }
 
             IntervalTimer::handle(ctx).update(ctx, |timer, _| {
@@ -2540,42 +2606,46 @@ fn launch(ctx: &mut warpui::AppContext, app_state: Option<AppState>, launch_mode
                 maybe_register_app_as_login_item(ctx);
             }
         }
-        #[cfg_attr(target_family = "wasm", allow(unused_variables))]
         LaunchMode::CommandLine {
             command,
             global_options,
             ..
         } => {
+            log::info!("[BOOT] launch: CommandLine mode");
             cfg_if::cfg_if! {
                 if #[cfg(target_family = "wasm")] {
+                    log::error!("[BOOT] launch: cannot execute CLI command on web, panicking");
                     panic!("Cannot execute CLI command {command:?} on the web");
                 } else {
+                    log::info!("[BOOT] launch: calling agent_sdk::run");
                     if let Err(err) = crate::ai::agent_sdk::run(ctx, command.clone(), global_options.clone()) {
+                        log::error!("[BOOT] launch: agent_sdk::run FAILED: {:?}", err);
                         eprintln!("{err:#}");
                         report_error!(err);
                         std::process::exit(1);
                     }
+                    log::info!("[BOOT] launch: agent_sdk::run OK");
                 }
             }
         }
-        // Proxy should never reach launch() — it's a thin byte bridge.
+        // Proxy should never reach launch()
         LaunchMode::RemoteServerProxy => {
-            log::error!("Proxy mode should not use the launch() path");
+            log::error!("[BOOT] launch: Proxy mode should not use launch() path, exiting");
             std::process::exit(1);
         }
-        // Daemon: bind the Unix socket and register the ServerModel.
-        // initialize_app already set up everything else including crash
-        // reporting.
+        // Daemon
         #[cfg(unix)]
         LaunchMode::RemoteServerDaemon { identity_key } => {
+            log::info!("[BOOT] launch: RemoteServerDaemon mode, launching daemon");
             remote_server::unix::launch_daemon(&identity_key, ctx);
         }
         #[cfg(not(unix))]
         LaunchMode::RemoteServerDaemon { .. } => {
-            log::error!("RemoteServerDaemon is not supported on this platform");
+            log::error!("[BOOT] launch: RemoteServerDaemon not supported on this platform");
             std::process::exit(1);
         }
     }
+    log::info!("[BOOT] launch: exit");
 }
 
 /// Initializes the logger before running tests.
diff --git a/app/src/platform/mod.rs b/app/src/platform/mod.rs
old mode 100644
new mode 100755
index 87c4037..199262d
--- a/app/src/platform/mod.rs
+++ b/app/src/platform/mod.rs
@@ -2,6 +2,8 @@
 pub mod wasm;
 #[cfg(windows)]
 pub mod windows;
+#[cfg(target_env = "ohos")]
+pub mod ohos;
 
 pub fn init() {
     #[cfg(target_family = "wasm")]
diff --git a/app/src/root_view.rs b/app/src/root_view.rs
index 37db5d1..8a91a71 100644
--- a/app/src/root_view.rs
+++ b/app/src/root_view.rs
@@ -833,14 +833,19 @@ pub(crate) fn open_new_with_workspace_source(
     source: NewWorkspaceSource,
     ctx: &mut AppContext,
 ) -> (WindowId, ViewHandle<RootView>) {
+    log::info!("[BOOT] open_new_with_workspace_source: entry");
     let global_resource_handles = GlobalResourceHandlesProvider::as_ref(ctx).get().clone();
     let window_settings = WindowSettings::as_ref(ctx);
     let options = default_window_options(window_settings, ctx);
-    ctx.add_window(options, |ctx| {
+    log::info!("[BOOT] open_new_with_workspace_source: calling ctx.add_window");
+    let result = ctx.add_window(options, |ctx| {
+        log::info!("[BOOT] open_new_with_workspace_source: RootView::new (closure)");
         let mut view = RootView::new(global_resource_handles, source, ctx);
         view.focus(ctx);
         view
-    })
+    });
+    log::info!("[BOOT] open_new_with_workspace_source: returned window_id={:?}", result.0);
+    result
 }
 
 pub(crate) fn open_new_from_path(
@@ -1115,7 +1120,9 @@ pub(crate) fn open_new_window_get_handles(
 
 /// Opens a new window.
 fn open_new(_: &(), ctx: &mut AppContext) {
+    log::info!("[BOOT] open_new: dispatching open_new_window_get_handles");
     open_new_window_get_handles(None, ctx);
+    log::info!("[BOOT] open_new: done");
 }
 
 /// Opens a new window with a specific shell
diff --git a/app/src/settings/ai.rs b/app/src/settings/ai.rs
index effdf8a..3d563b5 100644
--- a/app/src/settings/ai.rs
+++ b/app/src/settings/ai.rs
@@ -157,7 +157,7 @@ impl VoiceInputToggleKey {
             OperatingSystem::Mac => all_keys,
             // For non-Mac platforms, we exclude the `Fn` key since it may not be correctly identified by winit.
             // In particular, we saw it is unidentified for a ThinkPad with a standard keyboard.
-            OperatingSystem::Windows | OperatingSystem::Linux | OperatingSystem::Other(_) => {
+            OperatingSystem::Windows | OperatingSystem::Linux | OperatingSystem::Ohos | OperatingSystem::Other(_) => {
                 all_keys
                     .into_iter()
                     .filter(|key| *key != VoiceInputToggleKey::Fn)
@@ -173,7 +173,7 @@ impl VoiceInputToggleKey {
             match OperatingSystem::get() {
                 OperatingSystem::Mac => ("Command", "Option"),
                 OperatingSystem::Windows => ("Windows", "Alt"),
-                OperatingSystem::Linux | OperatingSystem::Other(_) => ("Super", "Alt"),
+                OperatingSystem::Linux | OperatingSystem::Ohos | OperatingSystem::Other(_) => ("Super", "Alt"),
             };
 
         match self {
@@ -1809,7 +1809,7 @@ impl AISettings {
 
         let voice_input_toggle_key = match OperatingSystem::get() {
             OperatingSystem::Mac => VoiceInputToggleKey::Fn,
-            OperatingSystem::Windows | OperatingSystem::Linux | OperatingSystem::Other(_) => {
+            OperatingSystem::Windows | OperatingSystem::Linux | OperatingSystem::Ohos | OperatingSystem::Other(_) => {
                 VoiceInputToggleKey::AltRight
             }
         };
diff --git a/app/src/settings/init.rs b/app/src/settings/init.rs
index 33df98d..d77acd3 100644
--- a/app/src/settings/init.rs
+++ b/app/src/settings/init.rs
@@ -53,32 +53,59 @@ pub struct UserDefaultsOnStartup {
 /// and hierarchy info for every setting. It does not set up appearance,
 /// rendering config, or event subscriptions.
 pub fn register_all_settings(ctx: &mut AppContext) {
+    log::info!("[BOOT] register_all_settings: entry");
     BlockListSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: BlockListSettings done");
     BlockVisibilitySettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: BlockVisibilitySettings done");
     DebugSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: DebugSettings done");
     SessionSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: SessionSettings done");
     KeysSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: KeysSettings done");
     FontSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: FontSettings done");
     TabSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: TabSettings done");
     WindowSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: WindowSettings done");
     SafeModeSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: SafeModeSettings done");
     TerminalSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: TerminalSettings done");
     PaneSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: PaneSettings done");
     CommandSearchSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: CommandSearchSettings done");
     AliasExpansionSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: AliasExpansionSettings done");
     CodeSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: CodeSettings done");
     LigatureSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: LigatureSettings done");
     GPUSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: GPUSettings done");
     ChangelogSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: ChangelogSettings done");
     GeneralSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: GeneralSettings done");
     AISettings::register_and_subscribe_to_events(ctx);
+    log::info!("[BOOT] register_all_settings: AISettings done");
     CloudAgentSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: CloudAgentSettings done");
     ScrollSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: ScrollSettings done");
     SelectionSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: SelectionSettings done");
     InputModeSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: InputModeSettings done");
     ThemeSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: ThemeSettings done");
     AccessibilitySettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: AccessibilitySettings done");
     NativePreferenceSettings::register(ctx);
+    log::info!("[BOOT] register_all_settings: NativePreferenceSettings done");
     CloudPreferencesSettings::register(ctx);
     WarpDrivePrivacySettings::register(ctx);
     UserAppInstallDetectionSettings::register(ctx);
@@ -113,40 +140,68 @@ pub fn init(
     startup_toml_parse_error: Option<user_preferences::Error>,
     ctx: &mut AppContext,
 ) -> UserDefaultsOnStartup {
-    ctx.add_singleton_model(|_| SettingsInitializer::new());
+    log::info!("[BOOT] settings::init: entry");
 
+    log::info!("[BOOT] settings::init: adding SettingsInitializer singleton");
+    ctx.add_singleton_model(|_| {
+        log::info!("[BOOT] settings::init: SettingsInitializer::new called");
+        SettingsInitializer::new()
+    });
+
+    log::info!("[BOOT] settings::init: calling register_all_settings");
     register_all_settings(ctx);
+    log::info!("[BOOT] settings::init: register_all_settings returned");
 
     // One-time migration: copy public settings from the platform-native store
     // into the TOML file so existing users don't lose their customizations
     // when the settings file feature is first enabled.
+    log::info!("[BOOT] settings::init: checking needs_settings_file_migration");
     if needs_settings_file_migration(ctx) {
+        log::info!("[BOOT] settings::init: needs migration, calling migrate");
         migrate_native_settings_to_settings_file(ctx);
+        log::info!("[BOOT] settings::init: migration done");
+    } else {
+        log::info!("[BOOT] settings::init: no migration needed");
     }
 
-    let use_thin_strokes = *FontSettings::as_ref(ctx).use_thin_strokes;
+    log::info!("[BOOT] settings::init: reading FontSettings");
+    let _before_font = FontSettings::as_ref(ctx);
+    log::info!("[BOOT] settings::init: FontSettings::as_ref done");
+    let use_thin_strokes = *_before_font.use_thin_strokes;
+    log::info!("[BOOT] settings::init: FontSettings field read done");
 
+    log::info!("[BOOT] settings::init: reading GeneralSettings");
     let general_settings = GeneralSettings::as_ref(ctx);
+    log::info!("[BOOT] settings::init: GeneralSettings read done");
+    log::info!("[BOOT] settings::init: reading tips_features_used");
     let tips_features_used = general_settings.welcome_tips_features_used.clone();
+    log::info!("[BOOT] settings::init: tips_features_used done");
     let tips_skipped_or_completed = *general_settings.welcome_tips_skipped_or_completed;
+    log::info!("[BOOT] settings::init: tips_skipped done");
     let user_default_shell_unsupported_banner_state =
         *general_settings.user_default_shell_unsupported_banner_state;
+    log::info!("[BOOT] settings::init: unsupported_banner done");
     let should_restore_session = *general_settings.restore_session;
+    log::info!("[BOOT] settings::init: restore_session done");
 
-    // Validate all public settings to detect values that parsed as TOML
-    // but cannot be deserialized into the expected Rust types.
+    log::info!("[BOOT] settings::init: validating public settings");
     let invalid_setting_keys =
         settings::SettingsManager::as_ref(ctx).validate_all_public_settings(ctx);
+    log::info!("[BOOT] settings::init: validation done");
     let settings_file_error = if let Some(err) = startup_toml_parse_error {
+        log::info!("[BOOT] settings::init: settings_file_error=FileParseFailed");
         Some(super::SettingsFileError::FileParseFailed(err.to_string()))
     } else if !invalid_setting_keys.is_empty() {
+        log::info!("[BOOT] settings::init: settings_file_error=InvalidSettings count={}", invalid_setting_keys.len());
         Some(super::SettingsFileError::InvalidSettings(
             invalid_setting_keys,
         ))
     } else {
+        log::info!("[BOOT] settings::init: settings_file_error=None");
         None
     };
 
+    log::info!("[BOOT] settings::init: building UserDefaultsOnStartup");
     let user_defaults_on_startup = UserDefaultsOnStartup {
         should_restore_session,
         tips_data: TipsCompleted::new(tips_features_used, tips_skipped_or_completed),
@@ -154,11 +209,16 @@ pub fn init(
         settings_file_error,
     };
 
+    log::info!("[BOOT] settings::init: reading GPUSettings");
     let gpu_settings = GPUSettings::as_ref(ctx);
+    log::info!("[BOOT] settings::init: GPUSettings::as_ref done");
     let prefer_low_power_gpu = *gpu_settings.prefer_low_power_gpu.value();
+    log::info!("[BOOT] settings::init: prefer_low_power_gpu={}", prefer_low_power_gpu);
     let backend_preference = *gpu_settings.preferred_backend.value();
+    log::info!("[BOOT] settings::init: backend_preference read done");
 
-    // Update the rendering config.
+    log::info!("[BOOT] settings::init: updating rendering config");
+    log::info!("[BOOT] settings::init: updating rendering config enter");
     ctx.update_rendering_config(|config| {
         config.glyphs.use_thin_strokes = use_thin_strokes;
         config.gpu_power_preference = if prefer_low_power_gpu {
@@ -168,7 +228,9 @@ pub fn init(
         };
         config.backend_preference = backend_preference;
     });
+    log::info!("[BOOT] settings::init: updating rendering config done");
 
+    log::info!("[BOOT] settings::init: subscribe_to_model FontSettings");
     ctx.subscribe_to_model(&FontSettings::handle(ctx), |font_settings, event, ctx| {
         if matches!(event, FontSettingsChangedEvent::UseThinStrokes { .. }) {
             let use_thin_strokes = *font_settings.as_ref(ctx).use_thin_strokes;
@@ -180,6 +242,7 @@ pub fn init(
 
     // Keep input_box_type in sync whenever honor_ps1 changes —
     // Classic when PS1 is honored, Universal otherwise.
+    log::info!("[BOOT] settings::init: subscribe_to_model SessionSettings");
     ctx.subscribe_to_model(
         &SessionSettings::handle(ctx),
         |session_settings, event, ctx| {
@@ -197,15 +260,17 @@ pub fn init(
         },
     );
 
+    log::info!("[BOOT] settings::init: calling appearance::register");
     appearance::register(ctx);
+    log::info!("[BOOT] settings::init: appearance::register done");
 
-    // Set up hot-reload for the settings file. When the WarpConfig watcher
-    // detects a change to settings.toml, reload preferences from disk and
-    // push changed values into setting models.
+    // Set up hot-reload for the settings file.
     #[cfg(feature = "local_fs")]
     {
+        log::info!("[BOOT] settings::init: checking WarpConfig for settings file hot-reload");
         let prefs = <settings::PublicPreferences as warpui::SingletonEntity>::as_ref(ctx);
         if prefs.is_settings_file() {
+            log::info!("[BOOT] settings::init: settings file detected, subscribing to WarpConfig changes");
             ctx.subscribe_to_model(
                 &crate::user_config::WarpConfig::handle(ctx),
                 handle_warp_config_change,
@@ -213,6 +278,7 @@ pub fn init(
         }
     }
 
+    log::info!("[BOOT] settings::init: exit");
     user_defaults_on_startup
 }
 
diff --git a/app/src/terminal/audible_bell/mod.rs b/app/src/terminal/audible_bell/mod.rs
index 04a9507..048e850 100644
--- a/app/src/terminal/audible_bell/mod.rs
+++ b/app/src/terminal/audible_bell/mod.rs
@@ -3,6 +3,7 @@
 use anyhow::Result;
 use warpui::{Entity, SingletonEntity};
 
+#[cfg_attr(target_env = "ohos", path = "ohos_audible_bell.rs")]
 #[cfg_attr(any(target_os = "linux", target_os = "freebsd"), path = "linux.rs")]
 #[cfg_attr(target_os = "macos", path = "macos.rs")]
 #[cfg_attr(target_os = "windows", path = "windows.rs")]
diff --git a/app/src/terminal/links.rs b/app/src/terminal/links.rs
index e68efed..e5ab567 100644
--- a/app/src/terminal/links.rs
+++ b/app/src/terminal/links.rs
@@ -7,7 +7,7 @@ use warpui::platform::OperatingSystem;
 pub fn directly_open_link_keybinding_string() -> &'static str {
     match OperatingSystem::get() {
         OperatingSystem::Mac => "Cmd +",
-        OperatingSystem::Linux | OperatingSystem::Windows => "Ctrl +",
+        OperatingSystem::Linux | OperatingSystem::Windows | OperatingSystem::Ohos => "Ctrl +",
         OperatingSystem::Other(_) => "Middle",
     }
 }
@@ -20,7 +20,7 @@ pub fn directly_open_link_keybinding_string() -> &'static str {
 pub fn should_directly_open_link(modifiers: &ModifiersState) -> bool {
     match OperatingSystem::get() {
         OperatingSystem::Mac => modifiers.cmd,
-        OperatingSystem::Linux | OperatingSystem::Windows => modifiers.ctrl,
+        OperatingSystem::Linux | OperatingSystem::Windows | OperatingSystem::Ohos => modifiers.ctrl,
         // On platforms other than MacOS, Linux, and Windows, accept both cmd and ctrl.
         OperatingSystem::Other(_) => modifiers.cmd || modifiers.ctrl,
     }
diff --git a/app/src/terminal/local_tty/shell.rs b/app/src/terminal/local_tty/shell.rs
index fb8bb71..21aec98 100644
--- a/app/src/terminal/local_tty/shell.rs
+++ b/app/src/terminal/local_tty/shell.rs
@@ -171,6 +171,9 @@ impl ShellStarter {
     }
 
     fn compute_fallback_shell() -> Option<ShellStarterSource> {
+        #[cfg(target_env = "ohos")]
+        return Self::compute_fallback_shell_ohos();
+
         cfg_if::cfg_if! {
             if #[cfg(unix)] {
                 let pw_shell_path = nix::unistd::User::from_uid(nix::unistd::getuid())
@@ -242,6 +245,45 @@ impl ShellStarter {
         }
     }
 
+    /// OHOS 专用：通过 SSH 桥接查询 VM 上的用户信息和 shell。
+    #[cfg(target_env = "ohos")]
+    fn compute_fallback_shell_ohos() -> Option<ShellStarterSource> {
+        use crate::platform::ohos::ohos_shell_bridge::{ohos_get_passwd, ohos_supported_shell};
+
+        let (_user, _dir, shell) = ohos_get_passwd()?;
+
+        // 优先使用 passwd 中的 shell（已在 VM 上验证存在）。
+        if let Some((resolved_path, shell_type)) = ohos_supported_shell(&shell) {
+            return Some(ShellStarterSource::UserDefault(DirectShellStarter {
+                args: arguments_for_session_spawning_command(
+                    resolved_path.as_path().to_string_lossy().as_ref(),
+                    shell_type,
+                ),
+                shell_path: resolved_path,
+                shell_type,
+            }));
+        }
+
+        let unsupported_shell = Some(shell);
+
+        // 回退到已知的 shell 路径（bash/zsh/fish 在 VM 上必然存在）。
+        let (resolved_path, shell_type) = ohos_supported_shell(BASH_SHELL_PATH)
+            .or_else(|| ohos_supported_shell(ZSH_SHELL_PATH))
+            .or_else(|| ohos_supported_shell(FISH_SHELL_PATH))?;
+
+        Some(ShellStarterSource::Fallback {
+            unsupported_shell,
+            starter: DirectShellStarter {
+                args: arguments_for_session_spawning_command(
+                    resolved_path.as_path().to_string_lossy().as_ref(),
+                    shell_type,
+                ),
+                shell_path: resolved_path,
+                shell_type,
+            },
+        })
+    }
+
     pub fn shell_type(&self) -> ShellType {
         match self {
             ShellStarter::Direct(starter) | ShellStarter::MSYS2(starter) => starter.shell_type(),
@@ -541,8 +583,17 @@ impl WslShellStarter {
 /// containing the resolved path to the binary and the corresponding `ShellType`. Else, returns
 /// None.
 pub fn supported_shell_path_and_type(path_or_command: &str) -> Option<(PathBuf, ShellType)> {
-    resolve_executable(path_or_command)
-        .and_then(|resolved_path| parse_shell_type_from_path(resolved_path.as_ref()))
+    #[cfg(target_env = "ohos")]
+    {
+        // OHOS: 不 resolve 本地文件系统（shell 在 VM 上），
+        // 而是通过 SSH 桥接直接返回已知 shell 类型。
+        return crate::platform::ohos::ohos_shell_bridge::ohos_supported_shell(path_or_command);
+    }
+    #[cfg(not(target_env = "ohos"))]
+    {
+        resolve_executable(path_or_command)
+            .and_then(|resolved_path| parse_shell_type_from_path(resolved_path.as_ref()))
+    }
 }
 
 /// If the given `path` is a supported shell binary, returns a tuple containing
diff --git a/app/src/terminal/local_tty/spawner.rs b/app/src/terminal/local_tty/spawner.rs
index 16204ad..ec921e4 100644
--- a/app/src/terminal/local_tty/spawner.rs
+++ b/app/src/terminal/local_tty/spawner.rs
@@ -1,3 +1,5 @@
+use std::ffi::OsString;
+
 use anyhow::Result;
 use warpui::{AppContext, Entity, SingletonEntity};
 #[cfg(unix)]
@@ -130,9 +132,9 @@ impl PtySpawner {
     pub fn new() -> Result<Self> {
         cfg_if::cfg_if! {
             if #[cfg(unix)] {
-                let server = super::server::TerminalServer::new()?;
+                let server = super::server::TerminalServer::new().ok();
                 Ok(Self {
-                    server: Some(server),
+                    server,
                 })
             } else if #[cfg(target_os = "windows")] {
                 Ok(Self {})
@@ -224,18 +226,35 @@ impl PtySpawner {
         >,
         is_crash_reporting_enabled: bool,
     ) -> Result<(PtySpawnResult, Box<dyn PtyHandle>)> {
-        let pty_spawn_info =
-            invoke_without_crash_reporting(is_crash_reporting_enabled, move || {
-                local_tty::spawn(
-                    options,
-                    #[cfg(windows)]
-                    event_loop_tx,
-                )
-            })?;
-        let direct_pty_handle = Box::new(DirectPtyHandle {
-            child: pty_spawn_info.child,
-        });
-        Ok((pty_spawn_info.result, direct_pty_handle))
+        #[cfg(target_env = "ohos")]
+        {
+            // OHOS: 使用 SSH 桥接代替 fork+exec。OhosPtyBridge 创建 PTY pair 后
+            // 启动子进程，通过 libssh2 连接到 VM 上的 bash。
+            let _ = is_crash_reporting_enabled;
+            let env_vars: Vec<(OsString, OsString)> = options.env_vars.into_iter().collect();
+            let (master_fd, handle) =
+                crate::platform::ohos::ohos_shell_bridge::OhosPtyBridge::spawn(&env_vars)?;
+            let result = PtySpawnResult {
+                pid: handle.pid(),
+                leader_fd: master_fd,
+            };
+            return Ok((result, Box::new(handle)));
+        }
+        #[cfg(not(target_env = "ohos"))]
+        {
+            let pty_spawn_info =
+                invoke_without_crash_reporting(is_crash_reporting_enabled, move || {
+                    local_tty::spawn(
+                        options,
+                        #[cfg(windows)]
+                        event_loop_tx,
+                    )
+                })?;
+            let direct_pty_handle = Box::new(DirectPtyHandle {
+                child: pty_spawn_info.child,
+            });
+            Ok((pty_spawn_info.result, direct_pty_handle))
+        }
     }
 
     #[cfg(unix)]
diff --git a/app/src/terminal/local_tty/unix.rs b/app/src/terminal/local_tty/unix.rs
index 99d1b29..578d603 100644
--- a/app/src/terminal/local_tty/unix.rs
+++ b/app/src/terminal/local_tty/unix.rs
@@ -116,41 +116,93 @@ pub fn get_pw_shell() -> String {
 ///
 /// If `buf` is changed while `Passwd` is alive, bad things will almost certainly happen.
 fn get_pw_entry(buf: &mut [i8; 1024]) -> Passwd<'_> {
-    // Create zeroed passwd struct.
-    let mut entry: MaybeUninit<libc::passwd> = MaybeUninit::uninit();
-
-    let mut res: *mut libc::passwd = ptr::null_mut();
-
-    // Try and read the pw file.
-    let uid = unsafe { libc::getuid() };
-    let status = unsafe {
-        libc::getpwuid_r(
-            uid,
-            entry.as_mut_ptr(),
-            buf.as_mut_ptr() as *mut _,
-            buf.len(),
-            &mut res,
-        )
-    };
-    let entry = unsafe { entry.assume_init() };
+    #[cfg(target_env = "ohos")]
+    {
+        // OHOS 沙箱中不能调 getpwuid_r（无本地 passwd），
+        // 通过 SSH 桥接从 VM 查询用户信息。
+        use std::ffi::CString;
+        let (name, dir, shell) = crate::platform::ohos::ohos_shell_bridge::ohos_get_passwd()
+            .expect("ohos_get_passwd should succeed on OHOS");
+
+        // 将三个字符串写入 buf（依次写入，用 \0 分隔）
+        let mut offset = 0;
+        for s in [&name, &dir, &shell] {
+            let c_str = CString::new(s.as_bytes()).expect("CString::new failed");
+            let bytes = c_str.as_bytes_with_nul();
+            let len = bytes.len();
+            if offset + len > buf.len() {
+                panic!("get_pw_entry: buffer too small for OHOS passwd");
+            }
+            buf[offset..offset + len].copy_from_slice(unsafe {
+                std::slice::from_raw_parts(bytes.as_ptr().cast::<i8>(), len)
+            });
+            offset += len;
+        }
 
-    if status < 0 {
-        panic!("getpwuid_r failed");
-    }
+        // 重构 &str 引用
+        let mut read = 0;
+        let name_str = unsafe {
+            let ptr = buf[read..].as_ptr().cast::<u8>();
+            let len = libc::strlen(ptr);
+            read += len + 1;
+            std::str::from_utf8(std::slice::from_raw_parts(ptr, len)).unwrap()
+        };
+        let dir_str = unsafe {
+            let ptr = buf[read..].as_ptr().cast::<u8>();
+            let len = libc::strlen(ptr);
+            read += len + 1;
+            std::str::from_utf8(std::slice::from_raw_parts(ptr, len)).unwrap()
+        };
+        let shell_str = unsafe {
+            let ptr = buf[read..].as_ptr().cast::<u8>();
+            let len = libc::strlen(ptr);
+            std::str::from_utf8(std::slice::from_raw_parts(ptr, len)).unwrap()
+        };
 
-    if res.is_null() {
-        panic!("pw not found");
+        return Passwd {
+            name: name_str,
+            dir: dir_str,
+            shell: shell_str,
+        };
     }
+    #[cfg(not(target_env = "ohos"))]
+    {
+        // Create zeroed passwd struct.
+        let mut entry: MaybeUninit<libc::passwd> = MaybeUninit::uninit();
+
+        let mut res: *mut libc::passwd = ptr::null_mut();
+
+        // Try and read the pw file.
+        let uid = unsafe { libc::getuid() };
+        let status = unsafe {
+            libc::getpwuid_r(
+                uid,
+                entry.as_mut_ptr(),
+                buf.as_mut_ptr() as *mut _,
+                buf.len(),
+                &mut res,
+            )
+        };
+        let entry = unsafe { entry.assume_init() };
 
-    // Sanity check.
-    assert_eq!(entry.pw_uid, uid);
+        if status < 0 {
+            panic!("getpwuid_r failed");
+        }
 
-    // Build a borrowed Passwd struct.
-    Passwd {
+        if res.is_null() {
+            panic!("pw not found");
+        }
+
+        // Sanity check.
+        assert_eq!(entry.pw_uid, uid);
+
+        // Build a borrowed Passwd struct.
+        Passwd {
         name: unsafe { CStr::from_ptr(entry.pw_name).to_str().unwrap() },
         dir: unsafe { CStr::from_ptr(entry.pw_dir).to_str().unwrap() },
         shell: unsafe { CStr::from_ptr(entry.pw_shell).to_str().unwrap() },
     }
+    }
 }
 
 pub struct Pty {
diff --git a/crates/warp_assets/Cargo.toml b/crates/warp_assets/Cargo.toml
index 2b82f2d..e0bf360 100644
--- a/crates/warp_assets/Cargo.toml
+++ b/crates/warp_assets/Cargo.toml
@@ -14,7 +14,8 @@ anyhow.workspace = true
 rust-embed.workspace = true
 warpui_core.workspace = true
 
-[target.'cfg(target_family = "wasm")'.dependencies]
-# Make sure we embed the data into the wasm binary even when running in debug
-# mode, as it cannot be read directly from the filesystem.
+[target.'cfg(any(target_family = "wasm", target_env = "ohos"))'.dependencies]
+# debug-embed ensures assets are compiled into the binary rather than read
+# from the filesystem at runtime. Required for wasm (no filesystem) and OHOS
+# (cross-compilation: filesystem on build host ≠ device).
 rust-embed = { workspace = true, features = ["debug-embed"] }
diff --git a/crates/warp_completer/src/completer/context/mod.rs b/crates/warp_completer/src/completer/context/mod.rs
index df3a13c..a6503c1 100644
--- a/crates/warp_completer/src/completer/context/mod.rs
+++ b/crates/warp_completer/src/completer/context/mod.rs
@@ -47,7 +47,7 @@ pub trait CompletionContext: Send + Sync {
         // where we don't know/have a running shell, we will go by the default shell per OS.
         match OperatingSystem::get() {
             OperatingSystem::Windows => EscapeChar::Backtick,
-            OperatingSystem::Linux | OperatingSystem::Mac | OperatingSystem::Other(_) => {
+            OperatingSystem::Linux | OperatingSystem::Mac | OperatingSystem::Ohos | OperatingSystem::Other(_) => {
                 EscapeChar::Backslash
             }
         }
diff --git a/crates/warp_core/src/operating_system_info.rs b/crates/warp_core/src/operating_system_info.rs
index ed97f3a..cb6c11a 100644
--- a/crates/warp_core/src/operating_system_info.rs
+++ b/crates/warp_core/src/operating_system_info.rs
@@ -126,12 +126,15 @@ pub enum OperatingSystemCategory {
     #[allow(dead_code)]
     Windows,
     Web,
+    Ohos,
 }
 
 impl OperatingSystemCategory {
     #[cfg_attr(target_family = "wasm", allow(dead_code))]
     fn new() -> Option<Self> {
-        if cfg!(any(target_os = "linux", target_os = "freebsd")) {
+        if cfg!(target_env = "ohos") {
+            Some(OperatingSystemCategory::Ohos)
+        } else if cfg!(any(target_os = "linux", target_os = "freebsd")) {
             Some(OperatingSystemCategory::Linux)
         } else if cfg!(target_os = "macos") {
             Some(OperatingSystemCategory::Mac)
@@ -152,6 +155,7 @@ impl Display for OperatingSystemCategory {
             OperatingSystemCategory::Mac => "macOS",
             OperatingSystemCategory::Windows => "Windows",
             OperatingSystemCategory::Web => "Web",
+            OperatingSystemCategory::Ohos => "OHOS",
         };
         write!(f, "{str}")
     }
diff --git a/crates/warp_core/src/paths.rs b/crates/warp_core/src/paths.rs
index 4c06bcc..d168523 100644
--- a/crates/warp_core/src/paths.rs
+++ b/crates/warp_core/src/paths.rs
@@ -333,6 +333,29 @@ pub fn bundled_resources_dir() -> Option<PathBuf> {
     }
 }
 
+// ── OHOS sandbox path functions ──
+// These provide access to the original sandbox paths passed from ArkTS via
+// set_ohos_environment() in ohos_entry.rs. They are only available after
+// start_warp_main() has initialized the environment.
+
+/// Returns the OHOS files directory (ArkTS context.filesDir), set as `HOME`.
+#[cfg(target_env = "ohos")]
+pub fn ohos_files_dir() -> Option<PathBuf> {
+    std::env::var("HOME").ok().map(PathBuf::from)
+}
+
+/// Returns the OHOS cache directory (ArkTS context.cacheDir), set as `XDG_CACHE_HOME`.
+#[cfg(target_env = "ohos")]
+pub fn ohos_cache_dir() -> Option<PathBuf> {
+    std::env::var("XDG_CACHE_HOME").ok().map(PathBuf::from)
+}
+
+/// Returns the OHOS temp directory (ArkTS context.tempDir), set as `TMPDIR`.
+#[cfg(target_env = "ohos")]
+pub fn ohos_temp_dir() -> Option<PathBuf> {
+    std::env::var("TMPDIR").ok().map(PathBuf::from)
+}
+
 #[cfg(all(test, feature = "local_fs"))]
 #[path = "paths_tests.rs"]
 mod tests;
diff --git a/crates/warp_features/src/lib.rs b/crates/warp_features/src/lib.rs
index 4baf95e..9e2e910 100644
--- a/crates/warp_features/src/lib.rs
+++ b/crates/warp_features/src/lib.rs
@@ -904,6 +904,13 @@ static FEATURES_INITIALIZED: AtomicBool = AtomicBool::new(false);
 /// Features used in debugging.
 pub const DEBUG_FLAGS: &[FeatureFlag] = &[FeatureFlag::DebugMode, FeatureFlag::RuntimeFeatureFlags];
 
+/// Features enabled for OHOS builds (disable desktop-specific features).
+#[cfg(target_env = "ohos")]
+pub const OHOS_FLAGS: &[FeatureFlag] = &[
+    // OHOS uses HiLog, not file-based logging, so crash reporting via files doesn't work.
+    // FeatureFlag::CrashReporting is disabled to avoid unnecessary panic hooks.
+];
+
 /// Features enabled for the development team.  The expectation is that, over
 /// time, these will move on to PREVIEW_FLAGS before being launched.
 pub const DOGFOOD_FLAGS: &[FeatureFlag] = &[
diff --git a/crates/warp_logging/src/lib.rs b/crates/warp_logging/src/lib.rs
old mode 100644
new mode 100755
index ca438f8..207f4c7
--- a/crates/warp_logging/src/lib.rs
+++ b/crates/warp_logging/src/lib.rs
@@ -30,6 +30,8 @@ mod imp;
 
 #[cfg(not(target_family = "wasm"))]
 mod rotation;
+#[cfg(target_env = "ohos")]
+pub mod ohos;
 
 pub use imp::init;
 #[cfg(not(target_family = "wasm"))]
diff --git a/crates/warp_logging/src/native.rs b/crates/warp_logging/src/native.rs
index bf898d6..fcd3891 100644
--- a/crates/warp_logging/src/native.rs
+++ b/crates/warp_logging/src/native.rs
@@ -649,7 +649,11 @@ pub fn log_directory() -> Result<std::path::PathBuf> {
 
 fn init_log_directory() -> Result<std::path::PathBuf> {
     cfg_if::cfg_if! {
-        if #[cfg(target_os = "macos")] {
+        if #[cfg(target_env = "ohos")] {
+            // OHOS: 文件日志不启用（使用 OhosLogger → HiLog），
+            // 但保留此分支以防外部调用需要返回一个合理路径。
+            Ok(std::path::PathBuf::from("/data/logs"))
+        } else if #[cfg(target_os = "macos")] {
             Ok(dirs::home_dir()
                 .ok_or_else(|| {
                     anyhow::anyhow!("could not locate home directory in order to create a log file")
diff --git a/crates/warpui/Cargo.toml b/crates/warpui/Cargo.toml
index 80bae94..fd91a76 100644
--- a/crates/warpui/Cargo.toml
+++ b/crates/warpui/Cargo.toml
@@ -153,6 +153,10 @@ version-compare.workspace = true
 winit.workspace = true
 wgpu.workspace = true
 
+[target.'cfg(target_env = "ohos")'.dependencies]
+openharmony-ability = { path = "../../../depends/openharmony-ability/crates/ability" }
+openharmony-ability-derive = { path = "../../../depends/openharmony-ability/crates/derive" }
+
 [target.'cfg(not(any(target_os = "macos", target_family = "wasm")))'.dependencies]
 global-hotkey = "0.7.0"
 
diff --git a/crates/warpui/src/platform/app.rs b/crates/warpui/src/platform/app.rs
index ffe34d1..456e80c 100644
--- a/crates/warpui/src/platform/app.rs
+++ b/crates/warpui/src/platform/app.rs
@@ -102,25 +102,30 @@ impl AppBuilder {
     /// initialize application state and be ready to process
     /// events from the main event loop.
     pub fn run(mut self, init_fn: impl FnOnce(&mut AppContext) + 'static) -> TerminationResult {
+        log::info!("[BOOT] AppBuilder::run: entry");
         let custom_tag_to_keystroke = self.custom_tag_to_keystroke_fn.take();
+        let default_keystroke_triggers = self.default_keystroke_trigger_for_custom_actions.take();
         // Wrap the initialization fn with one that first tries to convert custom triggers to
         // keystroke triggers.
         let init_fn = |ctx: &mut AppContext| {
-            if let Some(custom_tag_to_keystroke) = custom_tag_to_keystroke {
-                ctx.convert_custom_triggers_to_keystroke_triggers(custom_tag_to_keystroke);
+            log::info!("[BOOT] AppBuilder::run: init_fn wrapper called");
+            if let Some(f) = custom_tag_to_keystroke {
+                log::info!("[BOOT] AppBuilder::run: converting custom triggers to keystroke triggers");
+                ctx.convert_custom_triggers_to_keystroke_triggers(f);
             }
-            if let Some(default_keystroke_trigger_for_custom_actions) =
-                self.default_keystroke_trigger_for_custom_actions
-            {
-                ctx.register_default_keystroke_triggers_for_custom_actions(
-                    default_keystroke_trigger_for_custom_actions,
-                );
+            if let Some(f) = default_keystroke_triggers {
+                log::info!("[BOOT] AppBuilder::run: registering default keystroke triggers");
+                ctx.register_default_keystroke_triggers_for_custom_actions(f);
             }
+            log::info!("[BOOT] AppBuilder::run: calling user init_fn");
             init_fn(ctx);
+            log::info!("[BOOT] AppBuilder::run: user init_fn returned");
         };
 
-        if let Some(test_driver) = self.test_driver {
+        let result = if let Some(test_driver) = self.test_driver {
+            log::info!("[BOOT] AppBuilder::run: running with test_driver");
             self.inner.run(move |ctx, ui_app_future| {
+                log::info!("[BOOT] AppBuilder::run: inner.run(test) called");
                 init_fn(ctx);
 
                 ctx.foreground_executor()
@@ -131,8 +136,16 @@ impl AppBuilder {
                     .detach();
             })
         } else {
-            self.inner.run(|ctx, _| init_fn(ctx))
-        }
+            log::info!("[BOOT] AppBuilder::run: running without test_driver");
+            let result = self.inner.run(|ctx, _| {
+                log::info!("[BOOT] AppBuilder::run: inner.run closure called, calling init_fn");
+                init_fn(ctx);
+            });
+            log::info!("[BOOT] AppBuilder::run: inner.run returned, result={:?}", result.is_ok());
+            result
+        };
+        log::info!("[BOOT] AppBuilder::run: done");
+        result
     }
 }
 
diff --git a/crates/warpui/src/platform/linux/mod.rs b/crates/warpui/src/platform/linux/mod.rs
old mode 100644
new mode 100755
index 2df917f..923054a
--- a/crates/warpui/src/platform/linux/mod.rs
+++ b/crates/warpui/src/platform/linux/mod.rs
@@ -6,6 +6,7 @@ pub use crate::windowing::winit::app::App;
 use crate::windowing::{self, WindowingSystem};
 use crate::AppContext;
 
+#[cfg(not(target_env = "ohos"))]
 /// An extension trait defining additional configurability for
 /// applications when running on Linux.
 pub trait AppBuilderExt {
@@ -20,6 +21,7 @@ pub trait AppBuilderExt {
     fn force_x11(&mut self, force_x11: bool);
 }
 
+#[cfg(not(target_env = "ohos"))]
 impl AppBuilderExt for super::AppBuilder {
     fn set_window_class(&mut self, window_class: String) {
         match self.as_inner_mut() {
diff --git a/crates/warpui/src/platform/mod.rs b/crates/warpui/src/platform/mod.rs
old mode 100644
new mode 100755
index 05c6d37..f3d3a37
--- a/crates/warpui/src/platform/mod.rs
+++ b/crates/warpui/src/platform/mod.rs
@@ -3,6 +3,8 @@ pub mod app;
 pub mod linux;
 #[cfg(target_os = "macos")]
 pub mod mac;
+#[cfg(target_env = "ohos")]
+pub mod ohos;
 #[cfg(target_family = "wasm")]
 pub mod wasm;
 #[cfg(target_os = "windows")]
@@ -14,6 +16,8 @@ pub mod current {
     cfg_if::cfg_if! {
         if #[cfg(target_family = "wasm")] {
             pub use super::wasm::*;
+        } else if #[cfg(target_env = "ohos")] {
+            pub use super::ohos::*;
         } else if #[cfg(any(target_os = "linux", target_os = "freebsd"))] {
             pub use super::linux::*;
         } else if #[cfg(target_os = "macos")] {
diff --git a/crates/warpui/src/rendering/wgpu/mod.rs b/crates/warpui/src/rendering/wgpu/mod.rs
index 4060075..367ce30 100644
--- a/crates/warpui/src/rendering/wgpu/mod.rs
+++ b/crates/warpui/src/rendering/wgpu/mod.rs
@@ -143,7 +143,17 @@ fn get_wgpu_instance() -> Arc<wgpu::Instance> {
 
 /// Returns the set of wgpu backends that we can select from.
 fn wgpu_backend_options() -> wgpu::Backends {
-    wgpu::Backends::from_env().unwrap_or(wgpu::Backends::all())
+    #[cfg(target_env = "ohos")]
+    {
+        // OHOS 优先使用 Vulkan（Maleoon 916B GPU 支持 Vulkan），
+        // GL（OpenGL ES）作为回退。EGL 可能在部分设备上因 namespace
+        // 隔离不可用。
+        return wgpu::Backends::VULKAN | wgpu::Backends::GL;
+    }
+    #[cfg(not(target_env = "ohos"))]
+    {
+        wgpu::Backends::from_env().unwrap_or(wgpu::Backends::all())
+    }
 }
 
 #[cfg(not(target_family = "wasm"))]
diff --git a/crates/warpui/src/rendering/wgpu/resources.rs b/crates/warpui/src/rendering/wgpu/resources.rs
index d68ccf2..1e3d1c6 100644
--- a/crates/warpui/src/rendering/wgpu/resources.rs
+++ b/crates/warpui/src/rendering/wgpu/resources.rs
@@ -845,7 +845,12 @@ fn create_surface_config(
     // Use a non-vsync presentation mode for reduced input delay.  This could
     // cause visual tearing on present, but we're ok with paying that cost to
     // improve responsiveness.
-    config.present_mode = PresentMode::AutoNoVsync;
+    if cfg!(target_env = "ohos") {
+        // OHOS devices typically only support Fifo (vsync).
+        config.present_mode = PresentMode::Fifo;
+    } else {
+        config.present_mode = PresentMode::AutoNoVsync;
+    }
 
     // Explicitly request a non-opaque alpha compositing mode, if available.
     // Without this, transparent surfaces don't work on native Wayland.
diff --git a/crates/warpui/src/windowing/mod.rs b/crates/warpui/src/windowing/mod.rs
old mode 100644
new mode 100755
index 792c0d4..55f8d42
--- a/crates/warpui/src/windowing/mod.rs
+++ b/crates/warpui/src/windowing/mod.rs
@@ -1,5 +1,7 @@
 #[cfg(winit)]
 pub mod winit;
+#[cfg(target_env = "ohos")]
+pub mod ohos;
 
 pub use warpui_core::windowing::*;
 #[cfg(any(target_os = "linux", target_os = "freebsd"))]
diff --git a/crates/warpui/src/windowing/winit/app.rs b/crates/warpui/src/windowing/winit/app.rs
old mode 100644
new mode 100755
index 413ffbb..dd10642
--- a/crates/warpui/src/windowing/winit/app.rs
+++ b/crates/warpui/src/windowing/winit/app.rs
@@ -168,7 +168,7 @@ impl App {
 
         let mut event_loop_builder = winit::event_loop::EventLoop::with_user_event();
 
-        #[cfg(any(target_os = "linux", target_os = "freebsd"))]
+        #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))]
         if force_x11 {
             winit::platform::x11::EventLoopBuilderExtX11::with_x11(&mut event_loop_builder);
         }
@@ -182,7 +182,7 @@ impl App {
 
         // Perform some platform-specific initialization.
         cfg_if::cfg_if! {
-            if #[cfg(any(target_os = "linux", target_os = "freebsd"))] {
+            if #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))] {
                 super::linux::maybe_register_xlib_error_hook(&event_loop);
                 super::linux::ensure_cursor_theme();
             } else if #[cfg(target_family = "wasm")] {
diff --git a/crates/warpui/src/windowing/winit/event_loop/mod.rs b/crates/warpui/src/windowing/winit/event_loop/mod.rs
old mode 100644
new mode 100755
index f0364f6..3d1810f
--- a/crates/warpui/src/windowing/winit/event_loop/mod.rs
+++ b/crates/warpui/src/windowing/winit/event_loop/mod.rs
@@ -483,7 +483,7 @@ fn convert_touch_cancelled(
 /// A structure to manage state
 /// [`winit::event_loop::EventLoop`] and generate the appropriate callbacks into
 /// the UI framework.
-pub(super) struct EventLoop {
+pub(crate) struct EventLoop {
     ui_app: crate::App,
     callbacks: AppCallbackDispatcher,
     init_fn: Option<platform::app::AppInitCallbackFn>,
@@ -532,7 +532,7 @@ impl EventLoop {
 
         match evt {
             Event::NewEvents(StartCause::Init) => {
-                #[cfg(any(target_os = "linux", target_os = "freebsd"))]
+                #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))]
                 {
                     let windowing_system =
                         if winit::platform::x11::ActiveEventLoopExtX11::is_x11(window_target) {
@@ -975,7 +975,7 @@ impl EventLoop {
 
         let window = downcast_window(window.as_ref());
 
-        #[cfg(any(target_os = "linux", target_os = "freebsd"))]
+        #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))]
         if crate::windowing::winit::linux::take_encountered_bad_match_from_dri3_fence_from_fd() {
             log::warn!("Encountered a DRI3FenceFromFd error, forcing use of the NVIDIA GPU and recreating resources...");
             self.downrank_non_nvidia_vulkan_adapters = true;
diff --git a/crates/warpui/src/windowing/winit/linux/mod.rs b/crates/warpui/src/windowing/winit/linux/mod.rs
old mode 100644
new mode 100755
index 9f20d6e..288e691
--- a/crates/warpui/src/windowing/winit/linux/mod.rs
+++ b/crates/warpui/src/windowing/winit/linux/mod.rs
@@ -1,9 +1,11 @@
+#[cfg(not(target_env = "ohos"))]
 mod app;
 pub mod clipboard;
 mod cursor_theme;
 mod window_manager;
 mod zbus;
 
+#[cfg(not(target_env = "ohos"))]
 pub use app::{maybe_register_xlib_error_hook, take_encountered_bad_match_from_dri3_fence_from_fd};
 pub use clipboard::*;
 pub use cursor_theme::*;
diff --git a/crates/warpui/src/windowing/winit/mod.rs b/crates/warpui/src/windowing/winit/mod.rs
old mode 100644
new mode 100755
index 480876d..a05e29f
--- a/crates/warpui/src/windowing/winit/mod.rs
+++ b/crates/warpui/src/windowing/winit/mod.rs
@@ -1,6 +1,6 @@
 pub(crate) mod app;
 pub mod delegate;
-mod event_loop;
+pub(crate) mod event_loop;
 pub(crate) mod fonts;
 #[cfg(any(target_os = "linux", target_os = "freebsd"))]
 pub mod linux;
@@ -9,7 +9,7 @@ mod notifications;
 #[cfg(target_family = "wasm")]
 pub mod wasm;
 
-mod window;
+pub(crate) mod window;
 
 #[cfg(target_os = "windows")]
 pub mod windows;
@@ -17,7 +17,7 @@ pub mod windows;
 use app::CustomEvent;
 #[cfg(any(target_os = "linux", target_os = "freebsd"))]
 pub use app::WindowingSystem;
-use event_loop::EventLoop;
+pub(crate) use event_loop::EventLoop;
 #[cfg(any(target_os = "linux", target_os = "freebsd"))]
 pub use window::get_os_window_manager_name;
-use window::Window;
+pub(crate) use window::Window;
diff --git a/crates/warpui/src/windowing/winit/window.rs b/crates/warpui/src/windowing/winit/window.rs
old mode 100644
new mode 100755
index a604317..7492c22
--- a/crates/warpui/src/windowing/winit/window.rs
+++ b/crates/warpui/src/windowing/winit/window.rs
@@ -703,8 +703,8 @@ pub(super) const DEFAULT_TITLEBAR_HEIGHT: f32 = 35.0;
 /// A one-shot callback invoked with a captured frame. See [`platform::WindowContext::request_frame_capture`].
 type FrameCaptureCallback = Box<dyn FnOnce(platform::CapturedFrame) + Send + 'static>;
 
-pub(super) struct Window {
-    pub(super) callbacks: WindowCallbacks,
+pub(crate) struct Window {
+    pub(crate) callbacks: WindowCallbacks,
     inner: RefCell<Option<Inner>>,
     scene: RefCell<Option<Rc<Scene>>>,
     /// The height, in logical pixels, of the "titlebar region" at the top of the window.
@@ -1394,7 +1394,7 @@ fn create_window(
         FullscreenState::Normal => {}
     }
 
-    #[cfg(any(target_os = "linux", target_os = "freebsd"))]
+    #[cfg(all(any(target_os = "linux", target_os = "freebsd"), not(target_env = "ohos")))]
     if let Some(window_class) = _window_class.as_deref() {
         use winit::platform::x11::{WindowAttributesExtX11, WindowType};
 
diff --git a/crates/warpui_core/src/core/app.rs b/crates/warpui_core/src/core/app.rs
index 3707e45..3b0b7e7 100644
--- a/crates/warpui_core/src/core/app.rs
+++ b/crates/warpui_core/src/core/app.rs
@@ -2283,6 +2283,7 @@ impl AppContext {
     where
         F: FnOnce(WindowId, &mut AppContext) -> EntityId,
     {
+        log::info!("[BOOT] insert_window_internal: entry");
         let AddWindowOptions {
             window_style,
             window_bounds,
@@ -2295,7 +2296,11 @@ impl AppContext {
             window_instance,
         } = add_window_options;
 
-        let window_id = window_id.unwrap_or_else(WindowId::new);
+        let window_id = window_id.unwrap_or_else(|| {
+            log::info!("[BOOT] insert_window_internal: creating new WindowId");
+            WindowId::new()
+        });
+        log::info!("[BOOT] insert_window_internal: window_id={:?}", window_id);
 
         // Make sure we store the window bounds before we create the root view,
         // in case it uses this value.
@@ -2461,20 +2466,23 @@ impl AppContext {
             }),
         };
 
+        log::info!("[BOOT] insert_window_internal: calling WindowManager::open_window");
         let window_result = WindowManager::handle(self).update(self, |windowing_state, _ctx| {
             windowing_state.open_window(window_id, window_options, callbacks)
         });
 
         // Create the root view after adding the window but before registering an invalidation callback.
-        // This ensures that a platform window is always available to user view code.
+        log::info!("[BOOT] insert_window_internal: building root view (build_window_data)");
         let root_view_id = build_window_data(window_id, self);
+        log::info!("[BOOT] insert_window_internal: root_view_id={:?}, calling focus", root_view_id);
         self.focus(window_id, root_view_id);
 
         match window_result {
             Err(err) => {
-                log::error!("error opening window: {err}");
+                log::error!("[BOOT] insert_window_internal: WindowManager::open_window FAILED: {err}");
             }
             Ok(_) => {
+                log::info!("[BOOT] insert_window_internal: WindowManager::open_window OK, registering invalidation callback");
                 self.on_window_invalidated(window_id, move |window_id, ctx| {
                     if let Some(window) = ctx.windows().platform_window(window_id) {
                         // All we need to do when a window is invalidated is
@@ -2492,6 +2500,7 @@ impl AppContext {
             }
         };
 
+        log::info!("[BOOT] insert_window_internal: exit window_id={:?} root_view_id={:?}", window_id, root_view_id);
         (window_id, root_view_id)
     }
 
diff --git a/crates/warpui_core/src/platform/mod.rs b/crates/warpui_core/src/platform/mod.rs
index f5fae40..5a491e9 100644
--- a/crates/warpui_core/src/platform/mod.rs
+++ b/crates/warpui_core/src/platform/mod.rs
@@ -660,6 +660,8 @@ pub enum OperatingSystem {
     Mac,
     /// Windows.
     Windows,
+    /// OpenHarmonyOS / HarmonyOS NEXT.
+    Ohos,
     /// The operating system is unknown or not one of the ones specified.
     /// Contains the name of the operating system if it is known.
     Other(Option<&'static str>),
@@ -670,6 +672,8 @@ impl OperatingSystem {
         cfg_if::cfg_if! {
             if #[cfg(target_family = "wasm")] {
                 wasm::current_platform()
+            } else if #[cfg(target_env = "ohos")] {
+                OperatingSystem::Ohos
             } else if #[cfg(any(target_os = "linux", target_os = "freebsd"))] {
                 OperatingSystem::Linux
             } else if #[cfg(target_os = "macos")] {
@@ -682,6 +686,7 @@ impl OperatingSystem {
         }
     }
 
+    /// Returns true if the current [`OperatingSystem`] is Mac.
     /// Returns true if the current [`OperatingSystem`] is Mac.
     pub fn is_mac(&self) -> bool {
         *self == OperatingSystem::Mac
@@ -692,6 +697,11 @@ impl OperatingSystem {
         *self == OperatingSystem::Linux
     }
 
+    /// Returns true if the current [`OperatingSystem`] is OHOS.
+    pub fn is_ohos(&self) -> bool {
+        *self == OperatingSystem::Ohos
+    }
+
     /// Returns true if the current [`OperatingSystem`] is Windows.
     pub fn is_windows(&self) -> bool {
         *self == OperatingSystem::Windows
@@ -699,7 +709,7 @@ impl OperatingSystem {
 
     pub fn default_shell_family(&self) -> ShellFamily {
         match self {
-            OperatingSystem::Linux | OperatingSystem::Mac | OperatingSystem::Other(_) => {
+            OperatingSystem::Linux | OperatingSystem::Mac | OperatingSystem::Ohos | OperatingSystem::Other(_) => {
                 ShellFamily::Posix
             }
             OperatingSystem::Windows => ShellFamily::PowerShell,
