# Placeholder: libcore.so is prebuilt by script/bundle-ohos (Rust) and
# shipped via entry/libs/arm64-v8a/. This file exists only because
# build-profile.json5's externalNativeOptions requires a valid CMakeLists.txt;
# it must not build anything.
cmake_minimum_required(VERSION 3.5.0)
project(core_entry)
